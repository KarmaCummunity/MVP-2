---
description: MANDATORY — every agent that touches a feature must read and update docs/SSOT/BACKLOG.md + the relevant spec file
alwaysApply: true
---

# Project Status Tracking — Mandatory Workflow

## SSOT Structure

- **`docs/SSOT/spec/{domain}.md`** — single source of truth per feature domain (spec + status)
- **`docs/SSOT/BACKLOG.md`** — priority-ordered task queue
- **`docs/SSOT/TECH_DEBT.md`** — active technical debt register
- **`docs/SSOT/DECISIONS.md`** — architecture & product decisions

## Before you start any feature work

1. **Read** `docs/SSOT/BACKLOG.md` — confirm the feature isn't already done, and pick the highest-priority ⏳ item if none was assigned.
2. **Read** the matching `docs/SSOT/spec/{domain}.md` for the feature you're touching.
3. **Move** the task in BACKLOG.md from `⏳ Planned` → `🟡 In progress`.

## Spec Validation Gate

Before implementing ANY user request:
1. Read the relevant `docs/SSOT/spec/{domain}.md`
2. Compare the request against existing ACs
3. If contradiction → STOP and report to user
4. If new feature not in spec → report and ask to add
5. After implementation → update status in spec

## While working

- If you discover technical debt outside scope, append a row to `docs/SSOT/TECH_DEBT.md` under the right Active section (BE: `TD-50..99`, FE: `TD-100..149`). Do not refactor inline.
- If a product/architecture decision is made on the fly, append it to `docs/SSOT/DECISIONS.md`.

## Before you call the work done

You MUST update SSOT docs in the same change-set as your code:

1. **BACKLOG.md**: flip the status to `✅ Done` (or `🟡 In progress` if partial).
2. **spec/{domain}.md**: update the status header if all ACs for the domain are complete.
3. **TECH_DEBT.md**: close resolved TDs (move to Resolved) and add new ones.

## Verification gate

Every code-bearing response must prefix:

```
Mapped to spec: [FR-ID]. Refactor logged: [Yes/No/NA].
```

## What "touching a feature" means

Any of:
- Creating new domain/application/infrastructure code that maps to an `FR-*`.
- Editing a screen, hook, or store tied to an `FR-*`.
- Replacing mock data with real repository calls.
- Adding tests that change the AC-verified set for a feature.

## What does NOT need a status update

- Pure tooling changes (CI config, linter rules, dependency bumps with no behavior change).
- Documentation-only edits to spec files or this rule.
- Hotfixes that don't change any AC of an `FR-*`.

## If you cannot update BACKLOG.md

Stop. Either you don't have write access, the feature isn't tracked, or scope is unclear. Surface the issue to the user before proceeding — do not silently skip the update.
