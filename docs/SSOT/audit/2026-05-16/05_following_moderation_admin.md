# Audit 2026-05-16 — Following / Moderation / Super Admin

Scope: `FR-FOLLOW-*` (spec 03), `FR-MOD-*` (spec 08), `FR-ADMIN-*` (spec 12).
Excluded by EXEC-9: `FR-MOD-003` / `FR-MOD-004` / `FR-MOD-009` (block deferred post-MVP).
Format: `| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |`.
ID prefix `MOD-`.

## Findings

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
| --- | --- | --- | --- | --- | --- | --- |
| MOD-1 | 🔴 | FR-MOD-005 AC5 + FR-NOTIF-011 (user target) | `supabase/migrations/0064_notifications_auto_removed_trigger.sql:11`; `supabase/migrations/0047_report_admin_payload_enrichment.sql:79` | Auto-removal of a `target_type='user'` flips `users.account_status='suspended_admin'` but enqueues NO `auto_removed` notification — `posts_enqueue_auto_removed` only listens to `posts.status` transitions. Only an in-chat `owner_auto_removed` system message is injected (and the user is already locked out at sign-in, so they never see it). | Spec mandates a Critical notification to the owner for every target type. User targets get silent suspension; the user appeals into a void. | `BACKLOG.md` (new BE task) + spec amendment if user-target notification is intentionally chat-only. |
| MOD-2 | 🔴 | FR-MOD-005 AC2 (chat banner) | `supabase/migrations/0005_init_moderation.sql:31-52`; mobile: no producer | Auto-removed chats set `chats.removed_at = now()`; `is_chat_visible_to` then returns FALSE for both parties so the chat **disappears entirely** from inbox + transcript. There is no banner UI and no system message injected into the chat for either party (the `auto_removed` bubble lands only in the 3rd reporter's support thread). | Spec demands a "moderation banner" so both users learn the conversation was hidden; today they just see the thread vanish. Removes any recourse path. | `BACKLOG.md` (FE banner + BE per-party support-thread message) or spec amendment. |
| MOD-3 | 🔴 | FR-NOTIF-006/008 dedupe key | `supabase/migrations/0065_notifications_follow_triggers.sql:42,64`; `supabase/migrations/0056_notifications_outbox.sql:34` | Dedupe keys are `follow_req:<requester>:<target>` and `follow_appr:<requester>:<target>` with no cycle/timestamp segment. The unique partial index `notifications_outbox(dedupe_key) WHERE dedupe_key IS NOT NULL` is **never cleaned up** (no purge cron found), so a re-request after a 14-day cooldown (FR-FOLLOW-008) or a second approve cycle silently drops the notification. | Recipient gets no push/in-app for the second request → FR-FOLLOW-007 inbox is the only signal. Breaks FR-NOTIF-006 / FR-FOLLOW-008 end-to-end on the second cycle. | `TECH_DEBT.md` (BE, new TD-5x) — include cycle counter / created_at bucket in dedupe key. |
| MOD-4 | 🟠 | FR-ADMIN-007 AC2 (audit lookup) | `app/apps/mobile/app/settings/audit.tsx:39-55` | The audit screen uses `getUniversalSearchUseCase` to pick a user, which delegates to `users` RLS. After 0069 (D-21) the only SELECT policy is `users_select_active` — banned / `suspended_admin` / `suspended_for_false_reports` rows are hidden. Admin cannot reach exactly the rows they most need to audit. | Spec promises "last 200 audit events of any user (lookup by ID or display name)". For non-active users admin can only audit by pasting a UUID they already know — search is broken for the primary use case (post-mortem on a sanctioned user). | `TECH_DEBT.md` (TD-5x) — add admin-aware search RPC (`SECURITY DEFINER`) that bypasses `users_select_active` when `is_admin(auth.uid())`. |
| MOD-5 | 🟠 | FR-FOLLOW-002 AC1 (contextual copy) | `app/apps/mobile/src/components/profile/FollowButton.tsx:91-97`; `app/apps/mobile/src/i18n/locales/he/modules/profile.ts:21-22` | Unfollow confirm passes `body: ''` and `title: t('profile.unfollowConfirmTitle')` = "להפסיק לעקוב?". Spec requires "Stop following [name]?". No name interpolation. | Contradicts AC1 verbatim. Multi-account users cannot tell who they're about to unfollow from the confirm modal. | Spec-aligning FE fix (small) → `BACKLOG.md`. |
| MOD-6 | 🟠 | FR-MOD-002 vs `Report` shape | `supabase/migrations/0074_support_issue_rpc.sql:10-54`; `supabase/migrations/0005_init_moderation.sql:83-105` | FR-MOD-002 Related says `Domain: Report (with target_type = none)` and AC5 hinges on a `Report` row that does not increment counters. The implementation skips `reports` entirely — `rpc_submit_support_issue` only `inject_system_message`s. No row in `reports`, no `audit_events.action='report_target'` row for support issues, so FR-MOD-012 AC1 enumeration is wrong about which producers exist. | Spec/code divergence; analytics + audit timeline drops support submissions; the `reports.target_type='none'` enum value is dead. | Either spec amendment (drop `target_type='none'` from FR-MOD-002 Related & remove from CHECK) OR start writing the `none` row alongside the message → put in `TECH_DEBT.md`. |
| MOD-7 | 🟠 | FR-FOLLOW-001 AC4 / FR-FOLLOW-003 AC4 (UserUnavailable) | `app/packages/application/src/follow/GetFollowStateUseCase.ts:26-40`; `useOptimisticFollowAction.ts:100-102` | `followInteractionDisabled` is set only on `not_following_public` and `not_following_private_no_request`. State `cooldown_after_reject` already has its own disabled treatment, but `request_pending` and `following` (an active edge whose target was *later* suspended) carry no disabled hint → unfollow/cancel calls go through. On error all branches collapse to `t('profile.followErrorMessage')` (generic), masking `UserUnavailable` from the edge case in FR-FOLLOW-001. | UI doesn't reflect target suspension on existing edges; mis-leading toasts on suspended-target unfollow. | `TECH_DEBT.md` (FE, TD-1xx). |
| MOD-8 | 🟠 | FR-FOLLOW-009 / FollowersListScreen tap target | `app/apps/mobile/app/user/[handle]/followers.tsx:88-104` | The `⋮` "Remove follower" `TouchableOpacity` is nested inside the row's outer `TouchableOpacity` and never stops event propagation. Tapping the menu on iOS / Android first navigates to the follower's profile *then* opens the confirm modal. | Confusing UX; on iOS the modal opens behind the new screen and never receives input. | `TECH_DEBT.md` (FE, TD-1xx) — wrap inner `Pressable` with `onPress` that calls `e.stopPropagation()` and refactor the parent row. |
| MOD-9 | 🟠 | FR-MOD-001 reporter visibility prereq | `supabase/migrations/0005_init_moderation.sql:317-345` | `reports_validate_before_insert` requires `EXISTS` checks that ride the caller's RLS for posts/users/chats. After D-21 a user can never report a user whose `account_status<>'active'` (e.g. someone the admin temporarily suspended) — the existence check fails with `report_target_not_visible`. Same applies to `removed_admin` posts (RLS hides them) and `chats.removed_at IS NOT NULL` (visibility blocked). | Reporter gets a misleading "target not visible" instead of a guarded "already moderated". Also blocks legitimate dog-pile reports during the time window between auto-removal and admin review. | `TECH_DEBT.md` (BE, TD-5x) — surface a distinct `target_already_moderated` errcode or pre-check before the visibility EXISTS. |
| MOD-10 | 🟠 | FR-MOD-001 AC1 vs ReportUserModal | `app/apps/mobile/src/components/profile/ReportUserModal.tsx:4,54-61` | Uses bare `Alert.alert` for success / duplicate / generic errors. TD-138 already documented `Alert.alert` is a no-op on react-native-web 0.21.2 — the sibling `ReportPostModal` / `ReportChatModal` were migrated to `NotifyModal`. The user-report flow silently swallows feedback on the web build. | Reporter doesn't see "Report received" toast on web; appears broken; encourages duplicate reports → cooldown loop. | `TECH_DEBT.md` (FE, TD-1xx) — mirror the `NotifyModal` migration. |
| MOD-11 | 🟠 | FR-MOD-001 AC3 reset-on-close | `app/apps/mobile/src/components/ReportChatModal.tsx:1-57` | `ReportChatModal` has no `useEffect` clear (unlike `ReportPostModal:38-44` and `ReportUserModal:34-41`). Reopen-after-submit on the same mounted instance keeps the previous `reason`/`note`, so the second report inherits the first one's text. | Cross-target data leakage in the same session; user can accidentally submit stale text on the next target. | Small FE fix → `BACKLOG.md` or fold into a chat-modal cleanup task. |
| MOD-12 | 🟠 | FR-ADMIN-006 admin bypass on `is_post_visible_to` | `supabase/migrations/0005_init_moderation.sql:200-234`; `0049_admin_post_edit_rls.sql:23-27` | Posts table has `posts_select_super_admin` so admin CAN row-level SELECT a `removed_admin` post — but `is_post_visible_to()` itself (used by `feed_ranked_ids` / closure RPCs / batched joins) has no admin branch and returns FALSE. Anywhere a query relies on the helper rather than the policy, the admin sees nothing. | Inconsistent admin reach: row read works on `/post/[id]`, but feed-rank, related-posts, closure-candidate joins skip moderated posts even for admin. Symptom is "post still missing" after admin nav. | `TECH_DEBT.md` (BE, TD-5x) — add `or public.is_admin(p_viewer)` short-circuit and audit callers. |
| MOD-13 | 🟠 | FR-ADMIN-002 AC4 cascade restore double-bump | `supabase/migrations/0035_admin_restore_target.sql:73-83`; `0005_init_moderation.sql:514-518` | `admin_restore_target` stamps all open reports `dismissed_no_violation`. The status-change AFTER trigger bumps `users.false_reports_count` for **each** reporter, AND `reports_after_status_change_apply_sanctions` (0039) increments sanction level if the reporter crosses 5 dismissed in 30d. One admin click can sanction every reporter simultaneously (level guard prevents permaban escalation in one click, but each reporter still suspended for 7d at once). | Compounding sanction explosion on a borderline restore. Spec's "graduated" model (FR-MOD-010 AC2) is per-cycle, but cycle starts here are synchronized across all distinct reporters of the same target. | Operator runbook note + UI confirm wording at minimum; ideally a flag on `admin_restore_target` to skip dismissal counts. → `TECH_DEBT.md` (BE, TD-5x). |
| MOD-14 | 🟠 | FR-ADMIN-009 audit metadata | `supabase/migrations/0020_admin_remove_post.sql` (implied); `0035_admin_restore_target.sql:85-86` | `admin_restore_target` writes audit with `metadata = '{}'::jsonb`. No record of who was suspended/restored, no `distinct_reporters`, no `prior_status`. Compare with `auto_remove_target` (0047:88-90) which captures `distinct_reporters`. | Audit log for the **manual** restore path is poorer than the **auto** path, despite being more sensitive (admin discretion). Hard to post-mortem. | `TECH_DEBT.md` (BE, TD-5x) — capture `prior_status`, `report_count`, `distinct_reporters`. |
| MOD-15 | 🟠 | FR-MOD-005 AC1 dedup window vs threshold | `supabase/migrations/0005_init_moderation.sql:349-359, 412` | Same-reporter dedup uses a 24h window. The auto-removal `count(distinct reporter_id)` counts over `status='open'` regardless of age. A reporter who reports today, gets dismissed two months later, then files a fresh report tomorrow legitimately counts again. So far OK. But there is no upper bound on "open" age — a stale `open` report from 18 months ago that no admin ever resolved still counts toward the 3-distinct threshold today. | Spec AC1 is silent on staleness, but operationally a year-old `open` row from a dormant reporter can tip a target into auto-removal alongside two recent reports. No backstop in the schema. | `TECH_DEBT.md` (BE, TD-5x) — define an "open report freshness window" in spec + index. |
| MOD-16 | 🟢 | FR-FOLLOW-007 AC1 pagination cursor | `app/packages/infrastructure-supabase/src/users/follow/followRequestMethods.ts:113`; `getFollowers/Following` order by `created_at desc` with `cursor = follower_id` | `listPendingWithUsers` uses `lt('created_at', cursor)` — consistent. But `listFollowers` / `listFollowing` (`followMethods.ts:74,94`) order by `created_at desc` while paging via `.lt('follower_id', cursor)` (alphabetical UUID). Cursor and sort key disagree → page 2 can skip or duplicate rows around the boundary. Code comment acknowledges "good enough for a 50-row page in the MVP" but spec promises "paginated at 50 rows" for FR-FOLLOW-007 and FR-PROFILE-009. | Documented trade-off, but actively wrong for users with >50 followers (drops happen). Already partially tracked in `AUDIT_2026-05-10_FOLLOWUP.md` §1.6 family; not for this code path specifically. | `TECH_DEBT.md` (BE, TD-5x) — switch cursor to `created_at` encoded ISO and add tie-breaker on PK. |
| MOD-17 | 🟢 | FR-FOLLOW-005/006 simultaneous accept/cancel | `app/packages/infrastructure-supabase/src/users/follow/followRequestMethods.ts:56-82` | `acceptRequest`/`rejectRequest`/`cancelRequest` all do an UPDATE with `eq('status','pending')` and ignore the affected-row count. If target and requester act simultaneously (target approves, requester cancels) the first writer wins, the second UPDATE matches zero rows; both clients report success and reconcile to stale local state on next poll. | Edge case; user-visible glitch is "I cancelled but they got approved". No data corruption. | `TECH_DEBT.md` (FE/BE, TD-1xx) — surface `not_found_or_resolved` from adapter and reconcile. |
| MOD-18 | 🟢 | FR-FOLLOW state error mapping | `app/packages/infrastructure-supabase/src/users/follow/mapFollowError.ts:46-48` | PostgREST `42501` (RLS denial) is mapped to `FollowError('blocked_relationship')`. With blocking deferred (EXEC-9) the user can never actually hit a block, so 42501 now means "RLS rejected for some OTHER reason" (e.g. suspended account writing follow_edges per 0072 INSERT-active gate). UI says "blocked" — false signal. | Misleading error copy in the post-EXEC-9 world. Tech-debt parked behind blocking restoration. | `TECH_DEBT.md` (BE, TD-5x) — split 42501 mapping by table name (account-gate vs blocks). |
| MOD-19 | 🟢 | FR-MOD-012 AC2 audit metadata uniformity | `supabase/migrations/0037_admin_ban_delete_message.sql:52-54`; `0036_admin_dismiss_confirm.sql` (no own audit) | Audit `metadata` schema drifts: ban writes `{reason, note, permanent}`; restore writes `{}`; auto-remove writes `{distinct_reporters}`; dismiss writes `{reporter_id, target_type, target_id, reason}` (via 0005 status-change trigger). No JSON-schema discipline, no shared key vocabulary. | FR-ADMIN-007 row rendering must special-case each action; impossible to write a generic audit table view. | `TECH_DEBT.md` (BE+FE, TD-5x) — define metadata schema doc per action. |
| MOD-20 | 🟢 | FR-MOD-005 AC3 admin bubble target loss | `app/apps/mobile/src/components/chat/system/AutoRemovedBubble.tsx:65-83`; `BanUserModal.tsx` | The "Ban" inline action on the AutoRemovedBubble (target_type=user) hard-codes `reason: 'policy_violation'` and `note: 'auto-removed at threshold'`. Admin has no chance to choose — the explicit ban modal (FR-ADMIN-004) collects this, but the inline bubble shortcut bypasses it. | Operational divergence: ban reasons from the bubble are uniform garbage, audit becomes useless for filtering by reason. | Small FE refactor → open `BanUserModal` from the bubble instead of calling the use case directly → `BACKLOG.md`. |
| MOD-21 | 🟢 | FR-FOLLOW-009 AC3 cache freshness | `app/apps/mobile/app/user/[handle]/followers.tsx:60-66`; `useOptimisticFollowAction.ts:84-89` | After remove-follower the screen invalidates `['followers', me]` and `['user-profile', me]` only. The removed follower's `['profile-other', handle]` and `['follow-state', followerId, me]` are NOT invalidated. Removed party still sees "Following ✓" until they reload the profile. | NFR-PERF-005 freshness budget broken for the removed follower. They re-tap "Following" → unfollow no-op (edge already gone) → silent. | `TECH_DEBT.md` (FE, TD-1xx). |
| MOD-22 | 🟢 | FR-MOD-005 AC3 / FR-ADMIN-002 missing per-side cascade | `supabase/migrations/0047_report_admin_payload_enrichment.sql:204-229` | `auto_removed` system bubble is injected ONLY into the *3rd reporter's* support thread. Previous (1st, 2nd) reporters' threads keep showing `report_received` bubbles with no resolution signal even though the target was just auto-removed. | Information asymmetry; non-3rd reporters wonder if their report was acted on. | `TECH_DEBT.md` (BE, TD-5x) — broadcast `auto_removed` to every reporter's support thread. |
| MOD-23 | 🟢 | FR-ADMIN-006 AC2 invariant | `supabase/migrations/0010_seed_super_admin.sql`; no DB constraint | "Only one row may carry it [`is_super_admin = true`] at any time in MVP." No unique partial index `WHERE is_super_admin = true`. A second admin slip in (via SQL console or seed re-run) wouldn't be caught — `find_or_create_support_chat` uses `LIMIT 1` so reports silently route to whichever admin row sorts first, while the other admin's audit lookup still works. | Spec invariant unenforced; partition of report flow if two admins exist. | `TECH_DEBT.md` (BE, TD-5x) — `CREATE UNIQUE INDEX users_one_admin_idx ON users (is_super_admin) WHERE is_super_admin = true;`. |
| MOD-24 | 🟢 | FR-ADMIN-002 AC2 user→active vs verification | `supabase/migrations/0035_admin_restore_target.sql:58-59` | Restoring a `suspended_admin` user sets `account_status = 'active'` unconditionally, even if the user was `pending_verification` before being suspended (rare but possible — auto-removal can fire before they verify if 3 reporters hit them in pre-verification window). | Restore upgrades the account past the verification gate; the user becomes fully active without ever confirming email. | `TECH_DEBT.md` (BE, TD-5x) — store prior status in `audit_events.metadata` and reinstate it. |
| MOD-25 | 🟢 | EXEC-9 stale i18n string | `app/apps/mobile/src/i18n/locales/he/modules/moderation.ts:110-111` | `block_user`/`unblock_user` labels still ship in the moderation locale bundle. No code references them after EXEC-9 (grep finds only the locale file). Latent dead strings invite a future contributor to wire a "ביטול חסימה" button against a no-op flow. | Spec/code drift; small i18n bloat. | `TECH_DEBT.md` (FE, TD-1xx) — delete unused keys after block-restoration decision lands. |

## Cross-checks performed

- **EXEC-9 sweep**: `is_blocked` / `blockUser` / `unblockUser` / `is_blocked` reads are absent from mobile (only stale i18n strings remain — see MOD-25). `DeleteAccountConfirmModal`'s `isBlocked` is unrelated (delete-flow guard). Confirmed no UI surface depends on `public.blocks` rows.
- **TD-55 (FR-MOD-008 producers)**: only `posts_after_update_reopen_count` still wired (`0005:569`). No new producer landed; no new producer is broken.
- **TD-128 (SupabaseUserRepository file size)**: file currently 258 LOC, below the 300 cap. Follow-method extraction (TD-128) is effectively complete; only the repository remains "wide" by method count, not LOC.
- **D-17 admin snapshot floor**: rich-preview cards (`targetPreviewCard.tsx`) are gated by `useIsSuperAdmin()` on every bubble (`ReportReceivedBubble:32`, `AutoRemovedBubble:31`); the privacy floor holds **client-side**, but TD-59 (DB-level filter) is still open and matters here.
- **D-13 graduated sanctions**: 0039 trigger correctly uses `sanction_consumed_at` (FR-MOD-010 AC1 v0.3) — NOT `false_reports_count`. Level guard via optimistic `WHERE false_report_sanction_count = v_existing` prevents one-click permaban. Verified.
- **0076 suspension expiry cron**: lifts only `suspended_for_false_reports`; does NOT touch `suspended_admin` (intentional — admin-driven). `auth_check_account_gate` (0038) is the lazy fallback and emits an `unsuspend_user` audit on lazy lift. OK.
