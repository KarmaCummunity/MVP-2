# 07 — Mobile Frontend (cross-cutting)

Audit window: 2026-05-16 follow-up to AUDIT_2026-05-10. Read-only review — no code or spec edits.

Scope:
- `app/apps/mobile/app/**` (expo-router routes), `app/apps/mobile/src/**` (components, hooks, stores, services, i18n).
- `app/apps/mobile/app.json`, `app/apps/mobile/package.json`, `app/apps/mobile/public/.well-known/*`.
- Cross-cutting concerns only — domain-specific bugs go to files 01–06.
- Related TDs referenced: `TD-3` (deep-link race), `TD-9` (android gitignore + JAVA_HOME), `TD-11` (storage bucket public), `TD-29` (LOC allowlist), `TD-63` (notification icon placeholder), `TD-66` (deep-link manifest fingerprints), `TD-133/136` (search files), `TD-150` (no RNTL/jest), `TD-154` (no lint rule blocking Hebrew literals).
- Prior audit references: `AUDIT_2026-05-10_FOLLOWUP.md` §3.4 (clock skew), §4.3 (OAuth state), §4.6/§4.7 (AsyncStorage / soft-gate), §4.9 (app.json permissions), §5.11 (verification gate).

Severity: 🔴 HIGH · 🟠 MED · 🟢 LOW.

Row format: `| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |`.

---

## Build / tooling state at HEAD

- `pnpm typecheck` from `app/`: ✅ green (5/5 packages).
- `pnpm lint` from `app/`: ❌ **red** — 2 architecture violations (see FE-1, FE-2). The autonomous loop §13 "verify locally before push" gate is therefore not actually green at HEAD on `dev`. Either the loop did not run for the recent commits (`5035c36`, `f84c4a6`, `187539e`) or the gate was bypassed. This is independently a verification-gate finding (echo of `AUDIT_2026-05-10_FOLLOWUP.md` §5.11).
- Expo SDK has been bumped from `~53` to `~54.0.34` since the previous audit (package.json:30). Spec / docs in `CLAUDE.md` still say SDK ~53. No FR-AUTH or FR-CHAT impact verified, but the memory note about "SDK 53 vs Expo Go SDK 54 mismatch" is now stale on the SDK-53 side.

## Findings

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
| -- | -- | -------- | --------- | ------- | -------------- | ------------- |
| FE-1 | 🟠 | Architecture / lint:arch | `app/apps/mobile/app/(tabs)/create.tsx:1-450` | File is 450 LOC against the TD-29 allowlist max of 393. `node scripts/check-architecture.mjs` fails. The growth came from the recent "fix anonymous" commits (`f84c4a6`, `5035c36`) which added VisibilityChooser and PostActorExposurePicker wiring inline. | `pnpm lint` is red on the working branch — every PR that has `pnpm lint` as a required gate cannot merge cleanly. The architecture script intentionally fails on growth (per its own message: "the goal is to remove the entry, not raise it"). | New row under TD-29 in `TECH_DEBT.md` to split create.tsx (extract VisibilityChooser callbacks + actor-exposure handlers into a hook), OR a separate TD if the lane decides to bump the allowlist max temporarily. |
| FE-2 | 🟠 | Architecture / lint:arch | `app/apps/mobile/src/i18n/locales/he/modules/post.ts:1-216` | File is 216 LOC, above the 200 cap, and is **not** in `FILE_SIZE_ALLOWLIST`. `node scripts/check-architecture.mjs` fails. | Same lint:arch red as FE-1. Locale files split poorly when the message tree shares a namespace; this needs either a sub-module split (`post/actor.ts`, `post/menu.ts`, etc.) or an allowlist entry with a TD pointer. | New `TD-FE` row in `TECH_DEBT.md` to split `post.ts`. |
| FE-3 | 🔴 | Security / FR-AUTH-013 | `app/apps/mobile/src/services/authComposition.ts:53-66` + `app/apps/mobile/src/lib/container.ts:48-56` | On native iOS/Android, the Supabase auth session (access token + refresh token + PKCE code verifier) is persisted to **`AsyncStorage`**, which is plaintext (Keychain/Keystore are **not** used). The mobile manifest already declares `expo-secure-store` as a dependency (`package.json:33`) and as an Expo plugin (`app.json:59`), but `SecureStore` is **never imported or used anywhere** in the mobile code (`grep -rn "expo-secure-store" app/apps/mobile/src` returns nothing). A device with no PIN, or one rooted/jailbroken, leaks the refresh token. The same finding was raised as `AUDIT_2026-05-10_FOLLOWUP.md §4.7`; six commits later it is unchanged. | Refresh-token theft → long-lived account impersonation that survives password change (Supabase issues 1-week refresh tokens by default). Banned/suspended account gate (`FR-MOD-010`) does not help — the attacker uses the stolen token before the next 60 s gate poll. | Promote `AUDIT_2026-05-10 §4.7` to a P0/P1 `TD` row with a concrete fix: implement a `SupabaseAuthStorage` adapter backed by `expo-secure-store` on native (~10 KB cap is fine for a JWT), keep `localStorage` on web. Replace both `pickStorage()` factories. |
| FE-4 | 🔴 | Security / FR-NOTIF-* | `app/apps/mobile/src/lib/notifications/tapHandler.ts:7-10,23-29` | `handleNotificationTap` feeds `data.route` from the push payload **straight into `router.push({ pathname: data.route as never, params: data.params ?? {} })`** with no allow-list and no validation. The route string is cast `as never` to silence typed-routes. Any actor who can deliver an Expo push to a device (including a misconfigured Edge Function or a future external sender) can navigate the app to an arbitrary internal path with attacker-chosen params. Combined with `account-blocked?reason=…&until=…` and similar param-driven screens, this is a tap-to-pwn lever. | The Expo push channel is the trust boundary, not the JWT. Any compromise of the Expo push token (or the dispatch function) becomes an in-app navigation primitive. Even without compromise, a buggy backend payload navigates users to broken routes that may bypass `AuthGate` checks. | New `TD-FE` row in `TECH_DEBT.md` (🔴): replace `data.route` with a closed enum (`'chat' | 'post' | 'profile' | 'audit'`) resolved client-side to a typed route. Validate `data.chat_id` / `data.post_id` shape (UUID regex) before navigation. |
| FE-5 | 🟠 | Routing / TD-3 (deep-link race) — concrete reproducer | `app/apps/mobile/src/lib/notifications/tapHandler.ts:23-29` + `app/apps/mobile/src/components/AuthGate.tsx:140-167` | `useNotificationTapRouting` calls `Notifications.getLastNotificationResponseAsync()` inside the `useEffect` synchronously on mount and immediately fires `router.push(...)` if a cold-start tap exists. `AuthGate`'s effect simultaneously runs the redirect rules — and if the user is unauthenticated (cold-start session not yet restored), `AuthGate` calls `router.replace('/(auth)')`, clobbering the deep link. Even for an authenticated user, the push payload's `pathname` lands first; once `AuthGate` learns `onboardingState`, it can `router.replace('/(onboarding)/about-intro')` and the user loses the chat they tapped on. | TD-3 is currently labelled "no concrete reproducer." This is one: cold-start tap on a chat notification while signed out (push tokens persist across sign-out) sends user to the auth screen; the chat is dropped. | Downgrade TD-3 to "reproducer found" in `TECH_DEBT.md` with this paragraph as the repro. Fix: gate `handleNotificationTap` on `useAuthStore.getState().isAuthenticated && !isLoading && onboardingState === 'completed'`, queueing the pending tap until those conditions are met. |
| FE-6 | 🔴 | Security / TD-66 (deep-link manifests still placeholder) | `app/apps/mobile/public/.well-known/apple-app-site-association:5`, `app/apps/mobile/public/.well-known/assetlinks.json:7` | Six commits after TD-66 was opened, both files still contain literal `REPLACE_WITH_APPLE_TEAM_ID.com.karmacommunity.app` and `REPLACE_WITH_ANDROID_RELEASE_SHA256`. AASA also lists only `/auth/verify*` paths — `/auth/callback` (OAuth deep link), `/post/[id]`, `/user/[handle]`, `/chat/[id]` are not claimed at all. | Universal/App Links will not verify against either store. iOS will fall back to opening the URL in Safari; Android will show the "open with…" disambiguation. The OAuth round-trip and the `account-blocked` deep link both rely on AASA matching, so this is a launch blocker, not cosmetic. Email verification (FR-AUTH-006) silently degrades to web. | TD-66 stays open; add a P0 BACKLOG row "AASA/assetlinks fingerprints + path coverage" gated on the Apple Team ID / EAS release keystore (PM input). |
| FE-7 | 🟠 | Routing / FR-FEED-* (pagination dead-letter) | `app/apps/mobile/app/(tabs)/index.tsx:143-160` + `app/apps/mobile/src/components/PostFeedList.tsx:69-110` | The Home Feed passes `hasMore={Boolean(feedQuery.data?.nextCursor)}` to `<PostFeedList>` but **omits the `onEndReached` callback**. `PostFeedList` then renders a perpetual `<ActivityIndicator>` footer (line 92-97) on any feed that has a next cursor, but no call ever fires to fetch the next page. The user sees an endless spinner at the bottom of the feed once they scroll past page 1. | FR-FEED-001 / FR-FEED-008 implicitly assume the feed loads more than the first 20 posts. As written, the home feed is locked to the first page. The guest feed (`(guest)/feed.tsx:87-104`) and the chat inbox have the same shape — they at least don't show a misleading spinner because they don't pass `hasMore`. | New `TD-FE` row (🟠): wire `onEndReached` → `feedQuery.fetchNextPage()` (requires switching from `useQuery` to `useInfiniteQuery`), or hide the footer spinner when no `onEndReached` handler is bound. Add a domain test that asserts the home feed can paginate at least 2 pages. |
| FE-8 | 🔴 | Privacy / FR-PROFILE-* / FR-FEED-005 (multi-account leak) | `app/apps/mobile/src/store/filterStore.ts:84-104`, `app/apps/mobile/src/store/searchStore.ts:97-103`, `app/apps/mobile/src/store/lastAddressStore.ts:30-44` | All three `persist()` middlewares use **globally-scoped storage keys** (`'kc-filters'`, `'kc-search'`, `'kc-last-address'`) with no user-id namespace. Sign-in user A picks a city filter, sets a proximity-sort city, publishes a post with their home address → sign out → sign in user B on the same device → user B's app boots with A's filters, A's recent searches, and A's *exact home street + number* pre-filled in the Create Post form. AuthGate's `queryClient.clear()` (`AuthGate.tsx:127`) only clears in-memory React Query — the persisted Zustand stores survive. The filterStore comment claims "State is saved per signed-in user via AsyncStorage" — this is false. | A shared device (family, work, kiosk) leaks the previous user's location and search history. The `lastAddressStore` case is the worst — a home street + number is privacy-sensitive PII (`FR-POST-014 locationDisplayLevel` exists precisely to avoid leaking it), and pre-filling the Create form means a new user can accidentally publish someone else's address. | New `TD-FE` row (🔴): scope every persisted store's key by `userId` (or wipe them in `useAuthStore.signOut()` / on session-change to a different userId). Add a regression test that flips `setSession(A) → signOut() → setSession(B)` and asserts the persisted stores reset. |
| FE-9 | 🟠 | Build / FR-NOTIF-* | `app/apps/mobile/src/lib/notifications/register.ts:15-19,31-32` + `app/apps/mobile/app.json` (no `extra.eas.projectId`) | `registerCurrentDeviceIfPermitted` reads `Constants.expoConfig?.extra?.eas?.projectId`. `app.json` has no `extra` block. On every device boot, the code logs `'No EAS projectId configured — push token cannot be registered'` and returns. `getExpoPushTokenAsync` is never called, the device row is never inserted, and **no push notification is ever delivered to a real build**. | FR-NOTIF-* assumes push works. In dev (Expo Go) this fails silently. In production EAS builds the projectId is auto-injected by EAS — but only for builds done via EAS Build itself; expo-dev-client local builds do not get it. Anyone who builds locally (current loop iteration runs `expo run:ios`) hits this code path. The `console.warn` is not a banner. | New `TD-INFRA` row (🟠): either commit the project's EAS projectId to `app.json` (`extra.eas.projectId`) so local prebuild has it, or surface a user-visible diagnostic in `DevBanner` when push registration is skipped. |
| FE-10 | 🟠 | Hygiene / repo bloat | `/.turbo/cache/*` (21 files) + `.gitignore` | The recent commit `5035c36` includes `.turbo/cache/*` (manifest + meta + `.tar.zst`) directly in the git tree. `git ls-files .turbo` returns 21 entries. The root `.gitignore` does not exclude `.turbo`. | `pnpm test` / `pnpm typecheck` use the turbo cache; committing cache means every CI run from a fresh checkout may pick up stale hits, every dev push grows the history by `.zst` blobs, and the cache may contain machine-local paths. This was clearly accidental (the commit message is "try to fix anunimos 2 and fix hebrew"). | Add `.turbo/` to root `.gitignore`, then `git rm -r --cached .turbo` in a follow-up. Add a `pre-commit` hook that refuses to stage `.turbo/`. New `TD-INFRA` row. |
| FE-11 | 🟠 | RTL / i18n / TD-154 | `app/apps/mobile/src/features/about-landing/aboutExternalLinks.ts:5,13,16` | Three inline Hebrew literals in source code outside the locale files: `'פנייה מקהילת קארמה'` (mail subject), `'היי, אשמח לקבל קישור לקבוצת הווטסאפ של קארמה.'`, `'היי נוה, פנייה מקהילת קארמה.'` (WhatsApp prefills). Comment on line 1 explicitly says "no user-facing copy here." The strings are 100% user-facing — they land in the user's mail composer and WhatsApp draft. | TD-154 (no lint rule blocking Hebrew literals) is exactly to catch this. PM cannot retranslate the mail subject / WhatsApp invite from the locale bundle. | Move these strings to `i18n/locales/he/modules/aboutContentBundle.ts` or similar. Add to TD-154's reproducer list. |
| FE-12 | 🟠 | RTL / i18n | `app/apps/mobile/app.json:3,24-25,65` | `app.json` has hard-coded Hebrew strings for `expo.name` (`"KC - קהילת קארמה"`), iOS `NSCameraUsageDescription`, iOS `NSPhotoLibraryUsageDescription`, and the `expo-image-picker` `photosPermission`. These ship to the iOS native plist at prebuild time. There is no en/he split — App Store localized strings (CFBundleLocalizations / `*.lproj`) are not generated. | Functionally fine for a Hebrew-only MVP, but the strings are not testable against TD-154 (the lint scan ignores `*.json`). The audit's own grep flagged them. Also: any future English-locale support is blocked, because `expo.name` becomes the App Store display name and would need iOS localized variants. | Document the policy: "Hebrew-only literals allowed in `app.json` while `R-MVP-Core-4` (Hebrew only) holds." Otherwise, add a `localizedStrings/` infoPlist override. New `TD-FE` row to track, low priority. |
| FE-13 | 🟢 | i18n cleanup | `app/apps/mobile/src/i18n/index.ts:9` | `compatibilityJSON: 'v3'` is set. This is i18next v3 plural-forms compatibility — required when migrating from v22 to v23, deprecated for new projects, and removed in i18next v25. `package.json` pins i18next at `^23.16.0`. | Cleanup. Pluralization for Hebrew (which has 4 plural forms in CLDR but the app only ever uses singular/plural pairs) will subtly break when we eventually upgrade past v24. | New `TD-FE` (🟢): drop the `compatibilityJSON: 'v3'` line and migrate any `_plural`-suffixed keys to ICU `{count, plural, ...}` if any exist. |
| FE-14 | 🟢 | Routing / dead file | `app/apps/mobile/app/+html.tsx` | The file exists, but the root layout's comment (`app/_layout.tsx:11-13`) states: *"with `web.output: \"single\"` Expo builds from a default template that uses `expo.name`, so we can't use a `+html.tsx` (those only apply to `static` output)."* `web.output` in `app.json:42` is `"single"`. The `+html.tsx` is therefore unreachable; the RTL `<html dir="rtl">` / `lang="he"` / favicon work is done at runtime inside `_layout.tsx` instead. | Dead code — future maintainer reads `+html.tsx`, "fixes" something there, and is confused why the change doesn't show up. | Delete `+html.tsx` OR switch `web.output` to `static` and rely on it. New `TD-FE` (🟢). |
| FE-15 | 🟠 | Observability — no crash reporting | `app/apps/mobile/src/components/ErrorBoundary.tsx:32-37` | `componentDidCatch` only logs in `__DEV__`. Production builds catch the error and render the fallback UI but **never** report the error to any backend (no Sentry, no Crashlytics, no Bugsnag, no custom POST). `grep -rn "Sentry\|crashlytics\|datadog\|@bugsnag"` across `app/` returns nothing. | Once shipped, every crash is invisible until a user complains. P0 launch readiness gap; FR-AUTH / FR-CHAT regressions in production will be debugged blind. | New `TD-INFRA` row (🟠): wire Sentry (Expo has a first-party SDK with sourcemap upload via EAS). Alternatively a thin POST to a `report-error` Edge Function. |
| FE-16 | 🟠 | Performance / FlatList | `app/apps/mobile/src/components/PostFeedList.tsx:75-80` + `app/apps/mobile/src/components/PostCard.tsx`, `PostCardGrid.tsx` | `renderItem={({ item }) => <PostCardGrid post={item} onPressOverride={…} />}` is an inline arrow defined inside the parent's render — a new function identity every render. `PostCard` and `PostCardGrid` are **not** wrapped in `React.memo` (`grep -l "React\.memo\|export const.*= memo" src/components/*.tsx` returns nothing for any list-item card). Combined: every keystroke in the filter sheet, every realtime-INSERT bump of `newPostsCount`, every scroll-triggered state change re-renders every visible card. | FR-FEED-009 (realtime feed updates) and FR-FEED-005 (persisted filters) both trigger re-renders. On a low-end Android with 6 visible cards and Hebrew text shaping, this is measurable jank. The fix is well-known. | New `TD-FE` row (🟠): wrap card components in `React.memo` with a custom comparator on `post.postId + post.updatedAt`, and hoist `renderItem` to module scope or `useCallback`. Same pattern for `PostFeedList`, `chat/index.tsx`, `chat/[id].tsx`. |
| FE-17 | 🟠 | Performance / Image caching | `app/apps/mobile/src/components/PostCardGrid.tsx:51`, `PostCardProfile.tsx:64`, `PostImageCarousel.tsx:51,83`, `PostImageViewerModal.tsx`, `PostCard.tsx`, `RecipientPickerRow.tsx`, `AnchoredPostCardPreview.tsx`, `search/PostResultCard.tsx`, `CreatePostForm/PhotoPicker.tsx` | All post / avatar / closure thumbnails use bare `react-native` `<Image source={{ uri }} />`. No use of `expo-image`, no priority hint, no placeholder, no memory/disk cache budget. RN's default `Image` caches in-memory only and re-downloads when the URL drops out of cache (e.g. tab switch). | The feed page scrolls and each card may re-fetch its image from Supabase Storage. With TD-11 (storage bucket public, no CDN configured) this is direct egress per scroll. Visible jank + bandwidth burn. | New `TD-FE` row (🟠): replace all post/avatar `<Image>` with `expo-image` (already an Expo SDK first-party module with disk cache + blurhash + transition). |
| FE-18 | 🟠 | State management — stale closure | `app/apps/mobile/src/hooks/useFeedRealtime.ts:14-58` | The hook depends on `incrementNewPosts` but `onResume` is captured in the closure of the same `useEffect`. The effect intentionally disables `react-hooks/exhaustive-deps` (line 57). When the parent re-renders with a different `onResume` (e.g. because `feedQuery.refetch` identity changes after `useFilterStore` update), the `AppState` listener inside the effect still holds the **first** `onResume`. So after a filter change, returning from background calls the stale refetch which fetches the *previous* filter result, then immediately gets overwritten — but if the previous filter's React Query key has changed, the result is dropped on the floor. | Subtle bug — the user changes a filter, locks the screen, comes back > 60 s later, and the "new posts" pill increments against the wrong query. Worst case the home feed shows stale data until next manual refresh. | New `TD-FE` row (🟠): use a `useEvent`-pattern ref to keep `onResume` fresh without re-subscribing, or refactor the hook to take a ref. |
| FE-19 | 🟠 | Routing — typed-route escapes | `app/apps/mobile/app/settings.tsx:93,102,108,133`, `app/apps/mobile/app/settings/privacy.tsx:85`, `app/apps/mobile/app/user/[handle]/index.tsx:73,143-144`, `app/apps/mobile/src/components/profile/MyProfileOverflowMenu.tsx:20`, `app/apps/mobile/src/components/SettingsScreenRow.tsx:33`, `app/apps/mobile/src/components/post/PostMenuButton.tsx:58-59`, `app/apps/mobile/src/hooks/useEnforceAccountGate.ts:46-49` | Eleven call sites cast routes `as never`, `as any`, or `as unknown as Href` to bypass `expo-router`'s typed-routes (`experiments.typedRoutes: true`). The comment in `useEnforceAccountGate.ts` admits: "the new top-level route isn't picked up by the auto-generated typed-routes file (.expo/types/router.d.ts) until the Expo dev server runs and regenerates it." | The whole point of `typedRoutes` is to catch route renames at build-time. Every `as never` re-creates the bug class typed routes were meant to prevent. After a route rename, none of these will surface in `pnpm typecheck`. | New `TD-FE` row (🟢-🟠): document a `routes.ts` helper that wraps `router.push` with a hand-curated typed map, OR enable a pre-build step that regenerates `.expo/types/router.d.ts` so the `as never` escapes become unnecessary. |
| FE-20 | 🟠 | Cold-start / FR-AUTH-007 (stale-user flash) | `app/apps/mobile/src/components/AuthGate.tsx:48-83` + `app/apps/mobile/src/store/authStore.ts:30-37` | `authStore.isLoading` starts `true`, and the splash spinner blocks the tree until `setSession(...)` is called. But `setSession(null)` also flips `isLoading` to `false`. Between cold-start and first restored session, on web with `localStorage` warmed, AuthGate runs `subscribeToSession` *and* `getRestoreSessionUseCase().execute()` in parallel. The subscription's `INITIAL_SESSION` event can fire **before** `restore`'s promise resolves, calling `setSession(restoredSession)` first. If `restore` then resolves with a `null` session because of a transient PostgREST 401 (caught at line 80), `setSession(null)` overwrites the live session and the user is briefly signed out → redirected to `(auth)` → and the subscription's next tick signs them back in. Visible as a flash to the auth screen on cold start with intermittent network. | One-frame auth flicker on first load; the AuthGate `router.replace('/(auth)')` may already have committed before the second `setSession(real)` arrives. Reported in §3.4 of the prior audit obliquely (as "clock skew") but the mechanism is closure-based, not clock-based. | New `TD-FE` row (🟠): inside the `useEffect`, run `restore` first, *then* attach `subscribeToSession`. Or guard the subscription handler to ignore `null` deltas until restore has completed. |
| FE-21 | 🟢 | DRY — duplicated `pickStorage` | `app/apps/mobile/src/services/authComposition.ts:53-59`, `userComposition.ts:38-44`, `postsComposition.ts:~60`, `searchComposition.ts:~26`, `followComposition.ts:~33`, `lib/container.ts:48-54` | Six identical copies of `pickStorage()`. Each one independently picks `window.localStorage` on web or `AsyncStorage` on native. A subtle change in one place (e.g. switching to `SecureStore` for FE-3) would have to be made in six places. | Trivial bug-class — copy/paste drift. | Move `pickStorage` to `src/lib/storage.ts` (or `infrastructure-supabase` since it's the SupabaseAuthStorage adapter) and import from there. New `TD-FE` (🟢). |
| FE-22 | 🟢 | TD-63 — notification icon | `app/apps/mobile/assets/notification-icon.png` (514 bytes, dated 2025-05-14) | The notification icon is a 514-byte placeholder file unchanged for a year. TD-63 explicitly tracks this. | Production push notifications will render a generic/broken icon on Android (the icon is required to be a transparent PNG with the correct size, not a generic asset). | TD-63 stays open; surface in audit consolidation as still-blocked. |
| FE-23 | 🟢 | TD-29 — search.tsx allowlist at boundary | `app/apps/mobile/app/(tabs)/search.tsx` 634 LOC vs allowlist max 634 | The file is exactly at its allowlist max — any additional change to the search screen will fail `lint:arch`. TD-133/TD-136 already track splitting, but the boundary will block the next FR-FEED-017+ iteration. | Future feature work will be blocked at the lint:arch gate. | Note in TD-133 that "next change must reduce LOC, not preserve them." |
| FE-24 | 🟢 | Realtime — silent error swallow | `app/apps/mobile/src/store/chatStore.ts:150` | `onError: () => { /* deferred: surface via ChatChannelStatus hook */ }` — but no `ChatChannelStatus` hook is wired into `chat/[id].tsx` or the inbox. Errors from the realtime subscription are dropped on the floor. | Silent realtime disconnect. The user thinks a message did not arrive and re-sends. No reconnect attempt is visible. | New `TD-FE` (🟢): wire the `ChatChannelStatus` hook the comment promises, or remove the comment and surface via toast. |
| FE-25 | 🟢 | Performance / re-renders | `app/apps/mobile/app/(tabs)/index.tsx:38-41` | `useFilterStore()` (line 38) subscribes to the **entire** store — any setter call re-renders the whole `HomeFeedScreen`. The same store is selected slice-by-slice on line 39 via `useFilterStore((s) => s.activeCount())`. The unselectored subscription on line 38 makes the slice selector pointless. | Every filter mutation re-renders the home feed even when only `activeCount()` changed. | New `TD-FE` (🟢): replace `const filter = useFilterStore();` with `useShallow((s) => ({ type: s.type, … }))` matching the `feedFilter` memo on lines 46-67. |
| FE-26 | 🟢 | Verification gate (echo of §5.11) | `pnpm lint` red at HEAD (`dev` branch) | The autonomous loop §13 mandates "from `app/` run `pnpm typecheck && pnpm test && pnpm lint`. All three green or do not push." `pnpm lint` is red on `dev` at HEAD due to FE-1 and FE-2. The loop log either didn't run for `5035c36`/`f84c4a6`/`187539e` or skipped the gate. | Self-policing of the autonomous loop is broken. Hard to detect without a CI check on `dev`. | Add a required GitHub Actions check on `dev` that runs `pnpm lint`. Re-raise §5.11 priority. |
| FE-27 | 🟢 | Architecture — PKCE storage trust | `app/packages/infrastructure-supabase/src/client.ts:74-83` | `flowType: 'pkce'` is set, so the OAuth code verifier is stored in Supabase's storage adapter (AsyncStorage on native, localStorage on web). This replaces the explicit OAuth `state` parameter validation noted in `AUDIT_2026-05-10_FOLLOWUP.md §4.3`. The current implementation is correct from a protocol standpoint — but the security of the PKCE verifier depends entirely on the storage adapter being trusted. With FE-3 (AsyncStorage in plaintext) unresolved, a device-local attacker can read the PKCE verifier mid-flow and complete the OAuth dance. | Compound risk with FE-3. Mostly resolved by FE-3's `SecureStore` migration. | Tracked as a follow-on to FE-3, no separate TD needed. |
| FE-28 | 🟢 | Hygiene — TopBar tab navigator double-render | `app/apps/mobile/app/(tabs)/_layout.tsx:11-14` + `app/apps/mobile/app/_layout.tsx:96-141` | The (tabs) layout uses `<Tabs tabBar={() => null}>` and the **root** layout renders a global `<TabBar />` inside `ShellWithTabBar`. The expo-router `<Tabs>` is kept only "for per-tab state preservation" (comment line 2). This works but every tab switch re-runs `<Tabs>` plus the root `useShellTabBarVisibility` hook plus `<TabBar />` re-render. | Working as designed but the architecture deserves a `DECISIONS.md` note so future contributors don't try to "fix" the duplication. | New `D-*` entry in `DECISIONS.md` documenting the dual-bar pattern. |
| FE-29 | 🟢 | i18n — no English locale = no fallback testing | `app/apps/mobile/src/i18n/locales/he/` only — no `en/` | Only Hebrew exists. `fallbackLng: 'he'` (line 8) means a missing Hebrew key returns `'key.name'` literally instead of a translated fallback. Combined with no automated key-coverage test (TD-150 — no RNTL/jest), missing keys won't be caught until QA. (This audit's own ad-hoc scan found **0** missing keys, but only because the regex sweep had false positives — the methodology is not automatable as a CI check today.) | When the app eventually adds English (after R-MVP-Core-4 is lifted), every `t('foo')` call site must be re-audited. | New `TD-FE` (🟢): add a `vitest` test under `apps/mobile/src/i18n/__tests__/keys.test.ts` that statically extracts all `t('...')` keys from `app/**` + `src/**` and asserts they exist in the `he` bundle. |
| FE-30 | 🟢 | Hygiene — raw `throw new Error` in screens | `app/apps/mobile/app/(tabs)/create.tsx:136-137`, `app/apps/mobile/app/edit-post/[id].tsx:126-127` | `throw new Error('not_authenticated')` / `throw new Error('city_required')` are used as control-flow signals inside the screen. `domain/` has typed error classes specifically for this, but the screen layer doesn't use them — it catches the literal string code in a `try/catch` upstream. | The architecture lint already bans this in `packages/domain/` but not in `apps/mobile/`. Inconsistent with the policy in `CLAUDE.md §5` ("Error handling: domain code throws domain-specific error classes. Application/infrastructure layers catch domain errors and map to user-visible states"). Screens are neither — but they're the consumer of those mappings. | New `TD-FE` (🟢): extend `scripts/check-architecture.mjs` DOMAIN-ERROR-RULE to cover `apps/mobile/app/` and `apps/mobile/src/`. |

---

## Out of scope (deliberately not opened)

- FR-AUTH-014 guest preview pagination — guest feed is capped at 3 by design (`selectGuestPreviewPosts`); no pagination needed.
- WebView XSS — the only `react-native-webview` use is the Instagram embed with a hard-coded URL (`AboutInstagramEmbed.tsx:46`), no `injectedJavaScript`, no `dangerouslySetInnerHTML`, no `eval`. Safe.
- App.json permissions (`AUDIT_2026-05-10_FOLLOWUP.md §4.9`) — still just `CAMERA` and `READ_EXTERNAL_STORAGE` on Android; matches the camera/photo-picker code in `services/imageUpload.ts` and `services/avatarUpload.ts`. No over-permissioning.
- Clock skew (`§3.4`) — reviewed `useFirstPostNudge`, `useFeedRealtime`, and `chatStore`; all use `Date.now()` only for relative comparisons (`> 60_000` background timer). No absolute-time vulnerabilities introduced since 2026-05-10.
- expo-secure-store plugin / package present but unused — folded into FE-3 (security promotion, not a separate finding).

## Severity tally (this file)

- 🔴 HIGH: 4 (FE-3, FE-4, FE-6, FE-8)
- 🟠 MED: 14 (FE-1, FE-2, FE-5, FE-7, FE-9, FE-10, FE-11, FE-12, FE-15, FE-16, FE-17, FE-18, FE-19, FE-20)
- 🟢 LOW: 12 (FE-13, FE-14, FE-21, FE-22, FE-23, FE-24, FE-25, FE-26, FE-27, FE-28, FE-29, FE-30)

Total: 30 (cap met).
