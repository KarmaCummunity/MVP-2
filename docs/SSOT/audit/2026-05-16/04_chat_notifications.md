# 2026-05-16 Audit — Chat (FR-CHAT-*) + Notifications (FR-NOTIF-*)

Scope: `docs/SSOT/spec/07_chat.md` (FR-CHAT-001..016) + `docs/SSOT/spec/09_notifications.md` (FR-NOTIF-001..016). Code surfaces audited: `app/packages/{domain,application,infrastructure-supabase}/src/{chat,notifications}/*`, `app/apps/mobile/app/chat/*`, `app/apps/mobile/src/{components/chat,lib/notifications,store/chatStore*}`, `supabase/functions/dispatch-notification/*`, migrations `0011`, `0028`, `0031`, `0033`, `0047`, `0054`–`0060`, `0063`–`0066`, `0070`, `0072`, `0074`, `0076`, `0077`, `0079`, `0080`.

Severity legend in `README.md`. IDs are local to this file (`CHAT-*` / `NOTIF-*`).

---

## High

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
|----|-----|-----------|-----------|---------|----------------|---------------|
| NOTIF-1 | 🔴 | FR-NOTIF-015 AC3 | `app/apps/mobile/src/hooks/useSettingsAccountActions.ts:69-81`, `app/apps/mobile/src/lib/notifications/register.ts:27-39` | `deactivateCurrentDevice` is exported but never invoked outside `__tests__/`. `handleSignOut` calls `getSignOutUseCase().execute()` then `signOutLocal()` — the device row remains active, the next user signing in on the device shares it, and Expo pushes for the prior user continue until `bg-job-token-prune` removes it after 90 days. | Direct privacy / mis-delivery hole on every sign-out; also breaks the AC3 invariant the dispatcher relies on. | TD (BE/FE shared) → schedule a hotfix PR: call `deactivateCurrentDevice` before `signOutLocal()` and from the delete-account flow. |
| NOTIF-2 | 🔴 | FR-NOTIF-007 / 008 tap routing | `supabase/migrations/0065_notifications_follow_triggers.sql:60-66, 104-108`; `app/apps/mobile/app/user/[handle]/index.tsx:38-49`; `app/packages/infrastructure-supabase/src/users/SupabaseUserRepository.ts:145-147` | `follow_started` and `follow_approved` triggers emit `params: { handle: <user_uuid> }`. The OtherProfile route resolves `handle` via `findByHandle` which queries `share_handle` (not `user_id`) — tapping either notification lands on the "user not found" empty state. | Two of the three Social notifications are entirely broken on tap. AC2 for both FRs ("Tap → profile") fails. | TD (BE) → swap to a `SECURITY DEFINER` lookup of `share_handle` inside the trigger before building the payload, or route to a UUID-aware screen. |
| NOTIF-3 | 🔴 | FR-NOTIF-003 (closure system messages) | `supabase/migrations/0057_notifications_chat_trigger.sql:42-48`; `supabase/migrations/0031_post_closure_emit_system_messages.sql:100-104` | `messages_enqueue_notification` falls back to `participant_a` when `sender_id IS NULL` (system message). The closure trigger runs inside the owner's `AFTER UPDATE`; `auth.uid()` = owner. Because `enqueue_notification` self-suppresses, **only one** of the two participants receives the FR-NOTIF-003 push — whichever participant is *not* the owner *and* happens to be `participant_a`. When the owner is `participant_a`, the system push is self-suppressed and the counterpart gets nothing. | "Post closed" notifications are silently dropped for ~50% of chats based purely on UUID lexicographic order. Same bug fires for any other server-injected `sender_id IS NULL` message (admin report bubbles, donation-link reported, support-issue, mod-action-taken). | TD (BE) → derive recipient(s) explicitly: enqueue one notification per *non-actor* participant (`participant_a + participant_b`) instead of falling back to `participant_a`. |
| CHAT-1 | 🔴 | FR-CHAT-009 AC1/AC3 (block opacity) | `supabase/migrations/0072_rls_insert_requires_active_account.sql:65-79`; `supabase/migrations/0057_notifications_chat_trigger.sql:81-95` | `messages_insert_user` does **not** consult `has_blocked` — a counterpart that the viewer has blocked can still `INSERT` messages on the existing chat row. The message physically lands, fires `messages_enqueue_notification`, and produces a push to the blocker. RLS hides the chat (via `is_chat_visible_to`), but the push notification reaches the blocker, and on unblock the messages reappear retroactively. | Direct violation of `R-MVP-Privacy-3` / FR-CHAT-009 AC3 ("their send attempts succeed-locally and remain undelivered"). Right now they succeed *and* surface as push. | TD (BE) → add `not exists (… has_blocked …)` to `messages_insert_user`, OR reuse `is_chat_visible_to` in the notification trigger before enqueueing. |

## Medium

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
|----|-----|-----------|-----------|---------|----------------|---------------|
| CHAT-2 | 🟠 | FR-CHAT-002 AC2 (RTL bubble alignment) | `app/apps/mobile/src/components/MessageBubble.tsx:79-100` | `bubbleMine` uses `alignSelf: 'flex-start'` and `bubbleOther` uses `'flex-end'`. Hebrew (RTL forced per `R-MVP-Core-4`) is the only locale in MVP. Spec says "mine right-aligned in LTR … opposite for counterpart" — i.e. in RTL the user's own bubbles should be on the **left** end of the screen, which `flex-start` *does* yield once RN's RTL flip is applied, but the bubble-tail radii (`borderBottomLeftRadius` on mine vs `borderBottomRightRadius` on other) are also non-mirrored. The visual result depends on whether RN's I18nManager forces RTL — on web (`react-native-web`) it generally does **not**, so the bubbles render swapped versus the iOS app. | Inconsistent RTL between native and web; the spec's "mine right-aligned in LTR / opposite in RTL" is not actually enforced by code (depends on a layout side-effect). | TD (FE) → mirror via `I18nManager.isRTL` explicitly in styles or migrate to `marginStart` / `marginEnd` + RN-Web parity. |
| CHAT-3 | 🟠 | FR-CHAT-001 AC1 / FR-CHAT-013 (deleted-user dedupe) | `app/packages/infrastructure-supabase/src/chat/getMyChats.ts:26-40, 117-125` (and the test that locks in the behavior) | `dedupeRowsByCounterpart` buckets all chats with a `null` counterpart under one synthetic `__deleted__` key and keeps only the one with the latest `last_message_at`. If a user had threads with two different deleted accounts, one disappears from the inbox even though both should remain reachable per FR-CHAT-013 AC1 ("the thread remains accessible"). | Permanent data hide; user can no longer reach the older thread. | TD (FE) → key on `chat_id` (no dedupe) when counterpart is `null`. |
| CHAT-4 | 🟠 | FR-CHAT-011 AC1 (server-side read) + race with auto-mark-read | `app/apps/mobile/src/store/chatStoreInboxBump.ts:22-43`; `app/apps/mobile/src/components/useChatInit.ts:71-77` | When the user is actively on `/chat/:id` and a new incoming message arrives, the inbox-subscription bumps `unreadTotal` by 1 *before* the auto-`markChatRead` effect fires. The badge transiently flashes "1" and the row-level unreadCount jumps to 1, then back to 0. Same artifact for every fresh message while the user reads. | Visible badge flicker; minor UX bug but contradicts FR-CHAT-012 AC3 ("immediately optimistic"). | TD (FE) → in `bumpInboxForIncomingInsert`, suppress unread bump when the active route matches `/chat/${msg.chatId}`. |
| CHAT-5 | 🟠 | FR-CHAT-014 / FR-CHAT-015 (post_closed bubble) | `app/apps/mobile/src/components/chat/system/SystemMessageBubble.tsx:21-37`; `app/apps/mobile/src/components/MessageBubble.tsx:15-56` | `SystemMessageBubble` does not list `'post_closed'`. The renderer falls through to MessageBubble's neutral pill which shows the raw Hebrew `body` (`'הפוסט סומן כנמסר ✓ · תודה!'` etc.). Works visually because TD-148 keeps the body localized in DB, but: no localization layer, no rich card, and any future i18n change to TD-148 immediately ships an empty pill in chat. | Tight coupling between DB body text and UI; once TD-148 is closed (move bodies to translation table) all closure bubbles render blank. | TD (FE) → add a `'post_closed'` case to the dispatcher and render from `system_payload.status` so the bubble survives TD-148 cleanup. |
| CHAT-6 | 🟠 | FR-CHAT-012 inbox subscription resiliency | `app/packages/infrastructure-supabase/src/chat/SupabaseChatRealtime.ts:97-119` | `subscribeToInbox` calls `.subscribe()` with no status callback. `CHANNEL_ERROR` / `TIMED_OUT` go unhandled — the unread badge silently stops updating. There is no health hook or auto-reconnect. The chat-screen `subscribeToChat` accepts a callback but the consumer ignores it (`onError: () => { /* deferred */ }` in `chatStore.ts:150`). | Realtime drop → stale badge forever; no diagnostic signal to surface in dev or prod. | TD (FE) → surface channel status via a small zustand store; reconnect on `CHANNEL_ERROR` after a 5–10 s backoff. |
| CHAT-7 | 🟠 | FR-CHAT-003 (optimistic dedupe) | `app/apps/mobile/src/store/chatStore.ts:92-107` | `applyIncomingMessage` matches a pending optimistic by `status === 'pending' && senderId === msg.senderId && body === msg.body`. If the user sends the **same** body twice quickly (rapid double-tap or actual repeat), the second realtime INSERT reconciles onto the *first* pending optimistic, and the second optimistic stays "pending" forever. | Stuck "pending" bubble; minor but visible. | TD (FE) → also gate on `clientId` and/or strip the matched optimistic before the next match. |
| CHAT-8 | 🟠 | FR-CHAT-001 AC1 (inbox preview for system messages) | `app/apps/mobile/src/components/chat/InboxChatRow.tsx:33-36` | Inbox row preview is `item.lastMessage?.body ?? ''`. For server-injected system messages (post_closed, support_issue, mod_action_taken) the row shows the raw Hebrew sentence with no icon/marker. New chats with no messages show an empty preview row. | Visual confusion (looks like a counterpart message); no empty-thread copy. | TD (FE) → branch on `lastMessage.kind === 'system'` and render a muted "(הודעת מערכת)" prefix; show "(שיחה חדשה)" for empty threads. |
| NOTIF-4 | 🟠 | FR-NOTIF-001 AC2 / FR-NOTIF-002 (foreground suppression) | `app/apps/mobile/src/lib/notifications/foregroundHandler.ts:14-25` | Suppression only fires for `data?.kind === 'chat_message'`. `support_message` (FR-NOTIF-002) and `system_message` (FR-NOTIF-003) are **not** suppressed when the user is in the matching chat — the OS banner pops while the user is reading. | Spec violation: FR-NOTIF-002 AC1 says it shares the AC2 behavior of FR-NOTIF-001; FR-NOTIF-003 doesn't define AC2 explicitly but the dispatcher routes those into chat anyway. | TD (FE) → add `support_message` and `system_message` to the suppression branch; key on `data.chat_id` which all three emit. |
| NOTIF-5 | 🟠 | FR-NOTIF-005 AC1 (cron exactness) | `supabase/migrations/0066_notifications_post_expiry_cron.sql:19-24, 49-53` | Window `created_at + 293 days <= now() AND created_at + 294 days > now()` runs once per day at 06:00 UTC. If pg_cron misses one execution (Supabase outage, migration in flight, paused DB), the post falls outside the window and is **never** notified — there is no catch-up sweep. | Posts can silently miss the 7-day-before-expiry notification (FR-NOTIF-005 AC1: "exactly once"). | TD (BE) → widen the catch-up window to `< now()` excluding posts already in `notifications_outbox` with `dedupe_key='expire:<post_id>'` (or rely on the existing unique-index dedupe — outbox TTL is 7 d so re-emitting up to day 300 is safe). |
| NOTIF-6 | 🟠 | FR-NOTIF-001 AC3 (chat coalescing dedupe-key) | `supabase/migrations/0057_notifications_chat_trigger.sql:60-67`; `supabase/functions/dispatch-notification/index.ts:102-119` | The DB trigger writes `dedupe_key = 'chat:<chat>:<sender>:<minute>'` for chat and support messages. That blocks more than one outbox row from being enqueued per minute. The dispatcher's `coalesceChat` then queries the past 60 seconds and produces a "X new messages" body — but because the dedupe key already swallowed the duplicates, `priorCount` is always 0 in practice. Coalescing logic never engages. | FR-NOTIF-001 AC3 "multiple messages within 60 s collapse into a single OS notification with an updated count" — UI is technically correct (single notification) but never shows the count. Cosmetic. | TD (BE) → drop the per-minute dedupe key for `chat_message` so the dispatcher's coalescer can do its job; or change the dedupe window. |
| NOTIF-7 | 🟠 | FR-NOTIF-015 AC4 (90-day prune target) | `supabase/migrations/0058_notifications_dispatcher_glue.sql:40-44` | The prune job deletes any device with `last_seen_at < now() - 90 days`. But `last_seen_at` is only refreshed on `RegisterDeviceUseCase.upsert` (sign-in or chat-screen mount). A user who keeps the app open in the background for 91 days has their token pruned mid-session, then later receives no pushes until they sign in again or visit a screen that calls `registerCurrentDeviceIfPermitted`. | Subtle silent push outage for low-engagement-but-installed users. | TD (BE/FE) → add a daily AppState=active touchpoint that calls `register` to refresh `last_seen_at`. |
| NOTIF-8 | 🟠 | FR-NOTIF-005 / 011 / 010 dedupe over outbox TTL | `supabase/migrations/0056_notifications_outbox.sql:17-24, 33-35`; `0063_notifications_recipient_triggers.sql:54-56, 89-91` | `dedupe_key` is enforced by a unique partial index on `notifications_outbox`. `notifications_outbox_ttl` cron deletes rows after 7 days. After deletion, a previously deduped event can be re-enqueued because the unique constraint releases. Example: recipient marks/unmarks/re-marks > 7 days apart, or an admin restore-then-re-remove sequence. | Repeat notifications for events the spec treats as one-shot (FR-NOTIF-011: "Your post was removed" — re-firing after restore+re-remove is misleading; FR-NOTIF-005 unaffected because window is single-day). Low frequency, but a side effect of the outbox TTL design. | TD (BE) → keep a permanent dedupe table for kinds that need lifetime uniqueness (`auto_removed`, `unmark_recipient`). |
| NOTIF-9 | 🟠 | FR-NOTIF-001 AC2 (active screen tracking on web) | `app/apps/mobile/src/lib/notifications/tapHandler.ts:13-15`; `app/apps/mobile/app/_layout.tsx:88-103` | `tapHandler` short-circuits on `Platform.OS === 'web'` but the foreground handler is still installed and reads `useActiveScreenStore.getState().route`. Web users have no push (`Device.isDevice` returns false in `register.ts`), so this is dormant — but if Web Push ships under TD-65 without re-examining this glue, foreground suppression on web will misbehave silently. | Hidden trap for the TD-65 implementer; not a current user-visible bug. | TD (FE) → leave a `@throws` / TODO comment now; verify when Web Push lands. |
| NOTIF-10 | 🟠 | FR-NOTIF-014 AC2 / AC3 client-side enforcement | `app/apps/mobile/app/settings/notifications.tsx:50-69`; dispatcher `index.ts:82-88` | Dispatcher checks `prefs[row.category]` — correct server-side enforcement. But during the optimistic mutation in Settings (lines 56-67) the UI reflects the new toggle before the DB write completes; if the write fails the rollback restores the previous value, but a notification arriving between the toggle and the DB write can leak through because the server still sees the old value. | Tight race window (< 1 s); low-severity. | TD (FE) → block notifications client-side for the in-flight kind during the mutation. |

## Low

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
|----|-----|-----------|-----------|---------|----------------|---------------|
| CHAT-9 | 🟢 | FR-CHAT-002 (counter visibility threshold) | `app/apps/mobile/app/chat/[id].tsx:66-67, 155` | Counter only renders when `counter >= 1900` (100 chars from the 2000 cap). Spec says "longer are blocked with an inline counter" but doesn't define when it appears; current 100-char buffer is fine, but TextInput's `maxLength` already prevents over-typing — the counter is decorative. | Cosmetic only. | Inline note; no action. |
| CHAT-10 | 🟢 | FR-CHAT-005 AC4 (auto-message dedupe by post) | `app/packages/application/src/chat/OpenOrCreateChatUseCase.ts:13-21`; `app/packages/infrastructure-supabase/src/chat/supabaseDmChat.ts:85-129` | The implementation re-anchors the chat to the new post (per FR-CHAT-014 AC6) but does **not** scan `messages` for a prior "auto-message" with the same post. AC4 says "deep-links to the existing thread without re-prefilling" — code unconditionally accepts a `prefill` query param and pre-fills. There's no explicit dedupe; the user must clear the prefill manually if they don't want it. | Minor: per AC4, opening the same post twice should suppress the prefill the second time. | TD (FE) → check thread for a `chat_first_message`-style marker; alternatively store anchored-post-prefill state per `(chat_id, anchor_post_id)`. |
| CHAT-11 | 🟢 | FR-CHAT-016 / FR-CHAT-008 (D entry-point invariant) | `app/apps/mobile/app/(tabs)/donations/time.tsx` | FR-CHAT-008 AC1(d) (Volunteer composer routes to the same support thread) is still deferred to the local-intent fallback per TD-114; that's tracked, but the audit confirms the chat layer is ready (`rpc_submit_support_issue` exists). When the FR-DONATE-004 wiring lands, simply call `submitSupportIssue` with a `'Volunteer'` category. | Information only — closes a known follow-up. | Note for FR-DONATE-004 implementation. |
| CHAT-12 | 🟢 | FR-CHAT-016 AC4 (multi-deleted-chat dedupe) | `app/packages/infrastructure-supabase/src/chat/getMyChats.ts:34-40` | Tie-break uses string compare on `chat_id` — for UUIDs this is undefined-order but deterministic. AC4 spec doesn't define tie-break; current behavior is acceptable. | Documentation gap only. | Mention in `07_chat.md`. |
| CHAT-13 | 🟢 | FR-CHAT-007 AC2 (support thread reuse) | `supabase/migrations/0033_chat_inbox_personal_hide.sql:106-152`; `0011_chat_helpers.sql:44-92` | Two `rpc_get_or_create_support_thread` versions exist (0011 and 0033) — 0033 is the latest. Both correctly handle race via canonical ordering, but 0070 (security hardening) introduced an "atomic upsert" version. Three layered definitions with overlapping guards; consolidate. | Maintenance cost; no current bug. | TD (BE) → collapse to one definitive RPC. |
| CHAT-14 | 🟢 | FR-CHAT-001 AC3 (search semantics) | `app/apps/mobile/app/chat/index.tsx:46-53` | Spec says "case-insensitive prefix match" — code does `.toLowerCase().startsWith(needle)`. Hebrew letters have no upper/lower distinction, so toLowerCase is a no-op; the prefix semantic works. Edge case: `ְ`-class niqqud are not normalized — `"דוד"` won't match `"דָּוִד"`. | Cosmetic for current Hebrew-only MVP. | Inline note; consider NFKD normalize. |
| CHAT-15 | 🟢 | FR-CHAT-014 / FR-CHAT-002 (anchored card mounts ClosureSheet for non-owners) | `app/apps/mobile/src/components/chat/AnchoredPostCard.tsx:149-154` | `(isOwner \|\| isActiveChatClosure) ? <ClosureSheet/>` mounts the closure UI even when `isActiveChatClosure` is true for non-owners — but `closureInitiator` would have been set by an owner action, so this is effectively only true on the owner side. Harmless but the gating reads awkwardly. | Cosmetic. | Minor cleanup. |
| CHAT-16 | 🟢 | FR-CHAT-002 (admin long-press delete) | `app/apps/mobile/src/components/MessageBubble.tsx:60-77` | `onAdminLongPress` is wired on the bubble surface but with `delayLongPress={400}` — a long press also triggers the regular `onPress` because TouchableOpacity fires `onPress` after release regardless of long-press. Result: admin long-press → confirms delete AND toggles the time indicator. Minor flicker. | Cosmetic. | TD (FE) → cancel `onPress` when long-press fired. |
| NOTIF-11 | 🟢 | FR-NOTIF-016 (quiet hours) | `app/apps/mobile/app/settings/notifications.tsx:73-122` | Spec AC1 forbids exposing quiet-hours controls in Settings UI. Verified: settings screen exposes only `criticalLabel` + `socialLabel`. No leakage. | Confirms compliance. | No action. |
| NOTIF-12 | 🟢 | FR-NOTIF-012 (deletion email) | n/a — TD-118 | TD-118 already tracks the missing outbound email. Account-deletion calls Edge Function `delete-account` which never sends an email. | Already tracked. | No new action; verify TD-118 reflects the actual status. |
| NOTIF-13 | 🟢 | FR-NOTIF-015 AC1 (web register fallthrough) | `app/apps/mobile/src/lib/notifications/register.ts:8-25` | `Device.isDevice` is false in browsers → registration is silently skipped. AC1 mentions "FCM / APNs or Web Push" — Web Push is TD-65 deferred but the code does not log anything, so a web tester cannot tell whether registration was attempted. | Minor diagnostic gap. | TD (FE) → add a `console.info('Web Push not yet supported — TD-65')` log. |
| NOTIF-14 | 🟢 | FR-NOTIF-001..003 (push title/body localization) | `supabase/functions/dispatch-notification/i18n.json:1-23` | `i18n.json` ships only Hebrew strings — fine for MVP (`R-MVP-Core-4`). No fallback for unknown keys: `resolveString` returns the raw key. If a future trigger adds a new title_key that isn't in the JSON, the OS shows e.g. `notifications.unknownKey` literally. | Latent UX risk. | TD (BE) → add `console.warn` in `resolveString` when key is missing; CI check that every `enqueue_notification(*, …, key, …)` has a corresponding entry. |
| NOTIF-15 | 🟢 | FR-NOTIF-006 quick-actions | `supabase/migrations/0065_notifications_follow_triggers.sql:34-44`; `supabase/functions/dispatch-notification/index.ts:140-152` | Spec AC2 asks for OS quick actions "View / Approve / Reject" where supported. The dispatcher doesn't emit `categoryIdentifier` (iOS) or `actions` (Android). No FR violation in MVP (the spec says "where supported"), but functionality is silently absent. | Cosmetic / docs. | TD (BE+FE) → wire push categories for `follow_request`; document explicit deferral. |
| NOTIF-16 | 🟢 | Cron noise / on-empty behavior | `supabase/migrations/0077_notifications_backlog_alert.sql:11-31`; `0076_suspension_expiry_cron.sql:13-49`; `0066_notifications_post_expiry_cron.sql:11-41`; `0079`/`0080` | Verified: all five "scan-and-act" crons (post-expiry, suspension-expiry, backlog-alert, storage-orphan, exif-strip) are safe on empty input — they iterate `for r in select …` with no side effect when the set is empty, or call an Edge Function that should no-op. None throws on empty input. | Confirms green state. | No action. |

---

## Cross-check vs. prior audit (`AUDIT_2026-05-10_FOLLOWUP.md`)

- **FR-CHAT-011 AC4 (system mark-read)** — Verified `0054` + `0055` still in place. `rpc_chat_mark_read` is `SECURITY DEFINER`, uses `IS DISTINCT FROM`, and backfills `delivered_at`. **No regression.**
- **FR-CHAT-014 / 015 (anchored card + closure system)** — Verified the closure trigger fan-out (`0031`) and the AnchoredPostCard's `post_closed` invalidation effect. Re-anchor on entry (AC6) works via `rpc_chat_set_anchor`. **No regression.**
- **FR-CHAT-016 (personal inbox hide + dedupe)** — Verified the trigger that clears hide flags on new messages (`0033`) still fires for system messages too. Inbox dedupe works for the live-counterpart case but degrades for null counterparts (CHAT-3 above).
- **D-17 (admin report-bubble privacy floor)** — `useIsSuperAdmin()` is consistently gated in `ReportReceivedBubble`, `AutoRemovedBubble`, `ModActionTakenBubble`. No regression.

## Cross-check vs. open TD

- **TD-62** (message_undeliverable / FR-NOTIF-004): still deferred. Confirmed no client-side stub.
- **TD-148** (Hebrew system messages): still in `0031`. The CHAT-5 finding above amplifies the consequence.
- **TD-65** (Web Push parity): confirmed absent; NOTIF-9 + NOTIF-13 flag the dormant glue.
- **TD-152** (pre-migration report bubbles): backfilled in `0078`, verified the SystemMessageBubble handles null target_preview gracefully.

## Spec amendments worth considering

- **FR-CHAT-013 AC1** should explicitly state whether deleted-user threads dedupe in the inbox (today's code merges them — see CHAT-3). Confirm intent before fixing.
- **FR-NOTIF-003** lacks an `AC2 (foreground suppression)` analogue to FR-NOTIF-001 AC2 — NOTIF-4 currently has no spec hook to cite.
- **FR-CHAT-016 AC4** doesn't define the tie-break — see CHAT-12.
