# 06 — Donations, Statistics, Settings (FR-DONATE-*, FR-STATS-*, FR-SETTINGS-*)

Audit window: 2026-05-16 follow-up to AUDIT_2026-05-10. Read-only review — no code or spec edits.

Scope:
- Specs: `docs/SSOT/spec/13_donations.md`, `docs/SSOT/spec/10_statistics.md`, `docs/SSOT/spec/11_settings.md` (About is under Settings, per BACKLOG P2.6/P2.10).
- Recent decisions referenced: `D-16` (Donations + Search tabs), `D-27` (About narrative + optional anonymity).
- Related TDs: `TD-53` (Edge Functions deploy), `TD-118` (delete-account V1.1), `TD-135` (edge-cached community counter), `TD-155` (categoryLabelHe leak).

Severity: 🔴 HIGH · 🟠 MED · 🟢 LOW.

Row format: `| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |`.

---

## Donations (DON-*)

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
| -- | -- | -------- | --------- | ------- | -------------- | ------------- |
| DON-1 | 🔴 | FR-DONATE-008 (Edge Function — SSRF) | `supabase/functions/validate-donation-link/index.ts:41-77,124-131` | `parseUrl` only checks `^https?://`; the URL is then handed to `fetch(...)` server-side with `redirect: 'follow'`. No host allow-/deny-list, no block on private CIDRs (10/8, 172.16/12, 192.168/16, 127/8, 169.254/16, link-local IPv6), no port restriction, and no metadata-IP guard (e.g. `169.254.169.254` AWS IMDS / `metadata.google.internal`). A signed-in user can submit `http://169.254.169.254/latest/meta-data/` or any internal Supabase/Postgres URL and the Edge runtime will probe it. The 200/3xx/4xx/5xx-mean-reachable policy (line 68) also turns this into an internal-port scanner. | Server-side request forgery — internal-network probing from the dev/prod Supabase Edge runtime. Even if metadata endpoints are blocked at the Deno deploy network layer, the function is still a confused-deputy fetcher for arbitrary URLs with a real `User-Agent` header (line 60-64). The TD-53 closing-slice smoke ("edit a link, change display_name only") does not exercise this. | New `TD-BE` row in `TECH_DEBT.md` (🔴): add host validation (reject private/loopback IPs after DNS resolution, restrict ports to 80/443, cap redirects, abort on cross-host redirect into private space). Backlog item under P2.x security hardening. |
| DON-2 | 🟠 | FR-DONATE-003 AC2/AC4 | `app/apps/mobile/app/(tabs)/donations/money.tsx:14-26` | Spec FR-DONATE-003 AC2/AC3/AC4 mandate a primary CTA *"פתח את jgive.com ↗"* invoking `Linking.openURL('https://jgive.com')` with an inline error if `canOpenURL` is false. The current Money screen has NO jgive CTA — only the coming-soon body text and the `<DonationLinksList categorySlug="money" />`. i18n keys `donations.moneyScreen.openLink` and `linkErrorTitle`/`linkErrorBody` exist (`he/donations.ts:53-55`) but are unused. | A shipped AC is silently missing on the only money surface. Either the spec needs to be amended to make the CTA optional (matches D-16 "link-out only"), or the button must be re-added. | Spec amendment OR re-add CTA. Add row to `BACKLOG.md` (FR-DONATE-003 AC2 — restore jgive CTA) AND update spec footer if dropped intentionally. |
| DON-3 | 🟠 | FR-DONATE-001 AC1 / FR-DONATE-006 AC1 | `app/apps/mobile/app/(tabs)/donations/index.tsx:22-67` | Spec FR-DONATE-001 AC1 says **vertical stacked** tile order *items → time → money* (top to bottom in RTL), and FR-DONATE-006 AC1 requires a **divider** between the existing 3 modalities and the new 6 categories. Implementation renders **all 9 tiles in a 2-column grid with no divider** (rows: items+time, money+food, housing+transport, knowledge+animals, medical+spacer). Title "תרומות" is also missing from the render even though `styles.title` exists. | Visual ordering specified by AC1 is broken; categories no longer signal that they are a separate group. The "compact tile" prop bypasses the AC2 spacing intent. FE refactor likely lost this when moving to grid layout. | Spec amendment (if grid is preferred) OR FE fix. New `TD-FE` row in `TECH_DEBT.md` (🟠) referencing both ACs. |
| DON-4 | 🟠 | FR-DONATE-007 / TD-155 | `app/apps/mobile/src/components/DonationLinksList.tsx:75-79` | `canRemove` passed into `<DonationLinkRow>` is `link.submittedBy === me` only — super-admin is **not** considered, even though `useIsSuperAdmin()` is already in scope (line 31) and the menu handlers (`useDonationLinkActions`, `DonationLinkRowMenu`) correctly OR-in `isSuperAdmin`. `canRemove` is currently only used to choose the a11y label of the ellipsis (`DonationLinkRow.tsx:64`) but the inconsistent semantics will bite the next change. | Latent UX bug + inconsistency between row prop and menu logic. Easy to misread when adding new affordances (e.g. icon swap based on owner). | Opportunistic fix; flag as 🟢 TD-FE row (or fold into TD-155 cleanup since both touch this surface). |
| DON-5 | 🟠 | FR-DONATE-009 AC4 (DELETE semantics) | `app/packages/infrastructure-supabase/src/donations/SupabaseDonationLinksRepository.ts:98-110` | The spec was updated 2026-05-12 to hard `DELETE` instead of soft-hide (status banner, FR-DONATE-009 description). The repository still ships **both** `softHide` and `deleteById`. `softHide` is no longer referenced by app code (grep confirms `removeDonationLink` only calls `deleteById`) — dead code that writes `hidden_at`/`hidden_by`. Migration `0050_donation_links_purge_soft_deleted.sql` already purges legacy soft-hidden rows, so this method's UPDATE has no remaining purpose. | Dead code path that explicitly contradicts current spec semantics. Anyone calling `softHide` (e.g. a future caller) will hide rows that the UI no longer reveals (the `select_visible` policy still allows the submitter to see their own hidden row, but the list adapter filters `is('hidden_at', null)` — a stuck row). | New 🟢 `TD-BE` row to delete `softHide` method + tighten `IDonationLinksRepository` port. |
| DON-6 | 🟠 | FR-DONATE-008 AC3 / spec wording | `supabase/functions/validate-donation-link/index.ts:52-77` | Spec AC3 says: *"On 4xx/5xx or 405, retries once with GET. Treats status 200..399 as reachable."* Actual code accepts **any** status except 404 as reachable (line 68: `if (resp.status !== 404) return true;`). 401/403/500/503 all pass. Comment in code justifies this for Cloudflare/anti-bot pages. | Code drifts from spec but is intentional. Either (a) tighten back to spec, or (b) amend AC3 to "treat 404 as unreachable, all other HTTP responses as reachable" with rationale. Today's behavior allows broken links (e.g. host returns persistent 500) to be saved. | Spec amendment to match code, or BACKLOG entry to tighten code. |
| DON-7 | 🟠 | FR-DONATE-008 AC4 — error code parity | `app/packages/infrastructure-supabase/src/donations/SupabaseDonationLinksRepository.ts:55-63,86-94`; `supabase/functions/validate-donation-link/index.ts:11-12,206-210` | Spec AC4 enumerates these error keys for the inline modal: `invalid_url`, `unreachable`, `rate_limited`, `unauthorized`, `forbidden`, `network`, `unknown`. The Edge Function actually emits `server_error` (lines 173-174, 186-188, 206-208), and the client mapper does not include `server_error` in its union — it falls through to `unknown`. Also the client union lists `'invalid_input'` which the spec never mentions (Edge emits it for missing/invalid body fields, line 112/120-122). | The user-visible error on a DB write failure (the most operator-actionable case) is mistranslated to "unknown" instead of a translated server error. Audit gap between client union, Edge contract, and spec list. | New 🟢 `TD-FE` row to align client mapper + spec AC4 (rename or add `server_error` translation; add `invalid_input` to spec). |
| DON-8 | 🟠 | FR-DONATE-008 AC3 — rate-limit window | `supabase/functions/validate-donation-link/index.ts:179-191` | The rate-limit counts ALL of a user's rows created in the past hour, not only successful inserts. Because rate-limit is checked AFTER `checkReachable`, a flurry of failing inserts does not count (they never reach the table) — fine. But the count uses `created_at` on rows that may have been edited (rows persist; `validated_at` updates on edit). A user who edits 10 of their own old links in an hour does NOT trip the limit (edits skip the count branch, line 136-177). OK. **However**, RLS will block client-side INSERTs anyway, so the rate-limit check is the **only** abuse barrier. Coupled with DON-1, this is a thin defense. | Defense-in-depth nit. Document and accept, or move to per-IP + per-user combined limit. | Opportunistic 🟢 TD-BE. |
| DON-9 | 🟢 | FR-DONATE-008 / status code semantics | `supabase/functions/validate-donation-link/index.ts:34-39,84,89,98,112,...` | All error responses return HTTP 200 with `{ ok:false, code }` in the body, except `405 invalid_method`. This breaks generic HTTP error handling on the client and on monitoring/alerting; `functions.invoke` only populates the `error` slot on non-2xx. The contract is documented in the function header (lines 8-12) so this is intentional, but inconsistent (rate_limit/unauthorized would naturally be 429/401). | Hard to alert on. The 2026-05-10 audit may have noted this for other edge functions; if so, fold into the consolidated note. | 🟢 TD-BE: align edge-function HTTP semantics across `validate-donation-link`, `delete-account`, `dispatch-notification`. |
| DON-10 | 🟢 | FR-DONATE-007 AC1 — query filter | `app/packages/infrastructure-supabase/src/donations/SupabaseDonationLinksRepository.ts:35` | `listByCategory` still filters `.is('hidden_at', null)`. Since 0050 purged legacy soft-hidden rows and the app no longer writes `hidden_at`, this filter is now load-bearing only for historical edge cases. Harmless, but worth removing in the same PR that drops `softHide` (DON-5). | Cleanup. | Fold into DON-5 closing PR. |

---

## Statistics (STAT-*)

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
| -- | -- | -------- | --------- | ------- | -------------- | ------------- |
| STAT-1 | 🟠 | FR-STATS-002 AC1 — items_given accounting | `supabase/migrations/0006_init_stats_counters.sql:75-95,153-164` | Spec says items_given is +1 on `closed_delivered` OR `deleted_no_recipient`. Trigger implements this, AND also +1 on INSERT when a post is **seeded** directly in a closed state (lines 88-93). The 2026-05-12 closure path emits a system message via 0031; nothing in app code creates closed posts directly, but admin tooling could. Spec doesn't mention seeded-closed posts. | Edge case for backfill / admin tooling, but not user-visible. Document or accept. | Spec footnote; no TD. |
| STAT-2 | 🟠 | FR-STATS-001 AC2 — reactive update | `app/apps/mobile/app/stats.tsx:31-58` | "Counters update reactively (NFR-PERF-005) when underlying events fire" — implementation only invalidates `['user-profile', userId]` on `useFocusEffect`. There is no realtime subscription on `users` row changes; users see fresh counters only after navigating away and back. Activity timeline likewise refreshes only on focus or after the 20-second `staleTime`. | "Reactive" is broken if user posts → marks recipient → stays on stats. P0 KPI #1 metric ("items given") visibly lags. Accept and amend spec OR add a `users` row subscription (cheap, single PK = `user_id`). | 🟠 TD-FE row in `TECH_DEBT.md`. OR spec amendment to relax "reactive" to "refresh on screen focus". |
| STAT-3 | 🟠 | FR-STATS-004 AC2 — cached endpoint | `app/apps/mobile/app/stats.tsx:38-43`; `app/packages/infrastructure-supabase/src/stats/SupabaseStatsRepository.ts:64-77` | Spec says numbers refresh every 60s via a **single cached read endpoint** that FR-FEED-014 shares. Implementation reads the `community_stats` VIEW directly from the client every 60s; FR-FEED-014's "active posts" number is computed from the same view via `getActivePublicPostsCount → getCommunityStatsSnapshot()` (line 74-77) — i.e. two clients independently re-query the same view. There is no edge cache. TD-135 captures this for hardening; the stats spec still claims the cached endpoint. | Two surfaces (Stats screen + any FR-FEED-014 caller) double-query the same view every 60s. Acceptable at MVP scale per TD-135, but spec wording overstates the architecture. | Spec amendment cross-referencing TD-135 — OR keep TD-135 promise and fix later. |
| STAT-4 | 🟠 | FR-STATS-003 AC3 / data shape | `app/packages/infrastructure-supabase/src/stats/SupabaseStatsRepository.ts:79-104`; `app/apps/mobile/app/stats.tsx:134` | AC3 says "Tapping an entry deep-links to the relevant surface (post detail OR recipient view) when the resource still exists." Implementation always pushes `/post/[id]` regardless of kind. For `marked_as_recipient`/`unmarked_as_recipient` the entry has only `postTitle` and an `actorDisplayName`; there is no "recipient view" surface, so the recipient cases route into the post that has the recipient — defensible. But: there is no existence check on tap. If the post was hard-deleted (closure expiry), the user lands on a 404 screen. | Minor UX paper-cut. The RPC likely already filters deleted posts (the entries come from event-history join with `posts`), so the actual likelihood is low. Confirm and either tighten or document. | 🟢 TD-FE row: add 404 guard or rely on post-detail screen showing "post not found". |
| STAT-5 | 🟢 | FR-STATS-003 AC1 — limit policy | `app/packages/application/src/stats/ListMyActivityTimelineUseCase.ts:8-15` | Use case clamps to MAX=50, but spec AC1 says "Up to 30 most recent entries". Caller in `stats.tsx:47` passes the default 30. Spec wording is the hard cap; the use-case allows callers to ask for up to 50, which contradicts the spec ceiling. | Hardly load-bearing today (only one caller), but inviting future bugs. | 🟢 TD-AppLayer row: lower MAX to 30 OR amend spec to "up to 30 by default, capped at 50". |
| STAT-6 | 🟢 | FR-STATS-002 AC4 — clamp logging | `supabase/migrations/0006_init_stats_counters.sql:43-55` | `stats_safe_dec` raises `NOTICE` ('stats counter would go below zero — clamped') when a counter would go negative; spec AC4 says this should "trigger an alert (NFR-RELI-005)". Today there is no alerting wired to PG notices. FR-STATS-005's `stats_drift_events` table exists for the nightly path but the per-event clamp goes to logs only. | Spec promises ALERT, code only logs. Aligns with TD-134 (analytics spine) — same root cause. | Cross-ref to TD-134. |
| STAT-7 | 🟢 | FR-STATS-006 AC1 — leakage potential | `supabase/migrations/0006_init_stats_counters.sql:355-367` | `community_stats` view exposes total active **Public** posts and **all-time** `closed_delivered`. AC1 is satisfied. View granted to `anon, authenticated` — guests can read the community panel without auth. Spec FR-STATS-006 AC2 says "Community stats are accessible to any authenticated user" — implementation gives them to `anon` too. This is to satisfy FR-FEED-014 AC1 (guest peek number); but FR-STATS-006 AC2 doesn't say "anon allowed". | Spec contradiction (FR-FEED-014 vs FR-STATS-006). Both reference the same view. | Spec amendment to FR-STATS-006 AC2 — add explicit guest-read exception per FR-FEED-014. |
| STAT-8 | 🟢 | FR-STATS-001 AC3 — section ordering | `app/apps/mobile/app/stats.tsx:97-135` | Spec says: counters → timeline → community panel (top to bottom). Implementation renders **community panel first**, then personal stats strip, then activity timeline. | Visual contract drift. | Spec amendment (community-first feels right for a community app) OR FE re-order. |

---

## Settings (SET-*)

| ID | Sev | FR / Area | File:Line | Symptom | Why it matters | Proposed home |
| -- | -- | -------- | --------- | ------- | -------------- | ------------- |
| SET-1 | 🔴 | FR-SETTINGS-010 AC1/AC2 — legal links | `app/apps/mobile/app/legal.tsx:1-87`; `app/apps/mobile/app/settings.tsx:136-140` | Spec says "Two rows that open in-app web views with the canonical URLs" and "Both URLs are configurable via remote config." Implementation shows **static inline Hebrew text** for both Terms and Privacy in one screen — no web view, no URLs, no remote config. Settings menu links to `/legal` which only shows i18n strings (`legalContent.termsText`, `legalContent.privacyText`). | Legal compliance gap. EU/IL privacy law expects the actual canonical, versioned, dated terms — not an in-app stub that ships as part of the app bundle and can drift without re-acknowledgment. AC3 (re-acknowledge on newer version) is also unimplementable on inline strings. | High-severity backlog item (P2.x compliance) — implement web view + remote config OR file ADR de-scoping AC1/AC2/AC3 for MVP, with explicit risk note. |
| SET-2 | 🟠 | FR-SETTINGS-001 AC1 — section order | `app/apps/mobile/app/settings.tsx:78-181` | Spec order: Account, Notifications, Privacy, My Stats, Support, Legal, Logout, Delete Account. Implementation: Account, Notifications, Privacy, My Stats (`statsSection`), Support (includes Audit super-admin link, *and* Legal + About inside Support block), [Dev Tools], Logout+Delete combined block. Legal and About are not separate sections — they live as rows inside the "Support" group. | Spec calls out Legal as its own section per AC1. The grouping under Support hides the legal entry from users searching for it. Easy fix: hoist into its own section. | 🟠 BACKLOG entry (P2.x settings cleanup). |
| SET-3 | 🟠 | FR-SETTINGS-006 — Notifications toggle UX | `app/apps/mobile/app/settings.tsx:88-95` | Spec FR-SETTINGS-006 AC1/AC2/AC3 expect inline Critical and Social toggles **on the Settings screen itself** ("A short caption beneath each toggle"). Implementation renders a single row that navigates to a sub-screen (`/settings/notifications`). Sub-screen has the toggles — but the spec wording is "Toggles … inline" with captions in the Settings layout. | UX divergence: extra tap to see notification state. Sub-screen is justifiable (also hosts OS-permission status), but spec wording should be updated. | Spec amendment to FR-SETTINGS-006 (allow sub-screen) OR move toggles inline. |
| SET-4 | 🟠 | FR-SETTINGS-002 — Account section missing | `app/apps/mobile/app/settings.tsx:79-86` | Spec FR-SETTINGS-002 AC1/AC2/AC3: display read-only display_name + masked phone/email + auth method label, plus "Change password" for email users and an SSO note. Implementation: single row labeled "Account details" that links to `/edit-profile`. None of the identity fields, no auth-method label, no SSO note, no password-change affordance. | Whole AC group is unmet. Edit-profile is a different surface and lacks the password-change link the spec requires for email users. | 🟠 BACKLOG row for Account-section build-out (or amend spec to defer). |
| SET-5 | 🟠 | FR-SETTINGS-013 — read-only contact change | (no implementation found) | Spec FR-SETTINGS-013 AC1/AC2: tapping the masked phone/email row reveals a friendly modal with a Report-Issue CTA pre-categorized as Account. The masked phone/email row does not exist in the Settings UI (see SET-4) and there is no modal implementation. | Spec AC is unimplemented; would block the standard "I changed my phone number" support path. | Fold into SET-4 backlog item. |
| SET-6 | 🟠 | FR-SETTINGS-009 — backup contact email | (no implementation found) | Spec calls for a non-actionable line offering `mailto:support@karma.community` as a fallback. The settings screen does not render this. About → Contact has TWO mailto rows (`AboutContactLinks.tsx:38-39`) including `support@karma.community`, which satisfies the intent in spirit but lives on a different surface. **Memory note**: project policy is "no automated outbound email" and to use `mailto:karmacommunity2.0@gmail.com` for contact-us. `aboutExternalLinks.ts:8` uses `support@karma.community` (a domain not actually owned/operated per the rest of the codebase) — broken contact channel. | Spec violation + dead address. Mailing `support@karma.community` will silently fail. | 🟠 BACKLOG: replace `support@karma.community` with `karmacommunity2.0@gmail.com` across About/Settings; OR set up the domain. |
| SET-7 | 🟠 | FR-SETTINGS-011 AC1 — logout confirmation | `app/apps/mobile/src/hooks/useSettingsAccountActions.ts:69-81`; `app/apps/mobile/app/settings.tsx:169-173` | Spec: "Single tap with a confirmation modal: *Sign out of Karma Community?*". Implementation: tapping logout immediately calls `getSignOutUseCase().execute()` with **no confirmation**. Only the visible "Logging out…" label hints at the action. The hook has no Alert/Modal. | Easy mis-tap kicks the user to splash. Common UX pitfall. | 🟠 BACKLOG: wire `ConfirmActionModal` or `Alert.alert` (matches privacy-toggle pattern). |
| SET-8 | 🟠 | FR-SETTINGS-012 AC2 / V1.1 — delete-account post-actions | `supabase/functions/delete-account/index.ts:55-79`; TD-118 | Spec AC2 lists FollowEdge/FollowRequest hard-delete, push-token unregister, DeletedIdentifier cooldown row, and email confirmation. TD-118 explicitly says the V1 ship omits the cooldown table, the email, the orphan storage cleanup, and the recovery window. Confirming: the Edge Function does DB cleanup via `delete_account_data()` RPC + storage delete + `auth.admin.deleteUser` — but the function never logs an audit event for FR-SETTINGS-012 AC5, and the storage-delete error path is "log and continue" (line 60-69), leaving orphans world-readable in the public-read `post-images` bucket (overlaps TD-11). | Known gap (TD-118), but worth surfacing alongside the storage-orphan risk because the bucket is public-read. Re-rate TD-118 to 🔴 OR keep at 🟠 with mitigation note. | Re-evaluate TD-118 severity; possibly bump to 🔴 until V1.1 lands. |
| SET-9 | 🟠 | FR-SETTINGS-012 AC1 — confirmation phrase | `app/apps/mobile/src/components/DeleteAccountConfirmModal.tsx:48,71` | Spec AC1 says "typing the **user's display name** to confirm". Implementation requires typing the i18n keyword `settings.deleteAccountModal.confirmKeyword` (likely "מחק" or similar literal) — NOT the user's display name. | Spec contract drift. Typing a fixed keyword is weaker security (anyone with device access can guess the localized verb) than typing the actual display name. | 🟠 BACKLOG: align prompt with spec OR amend spec. |
| SET-10 | 🟠 | FR-SETTINGS-007 — My Stats shortcut placement | `app/apps/mobile/app/settings.tsx:113-120` | Spec FR-SETTINGS-007 AC1: label "My stats". Implementation labels the row via `t('settings.stats')` and the section header via `t('settings.statsSection')`. The section header above it (`statsSection` key) is redundant when the row itself is just "My stats" — visual noise that doesn't match the spec's single-row pattern under Privacy. | Cosmetic; section header should probably be removed for a single-row group. | 🟢 TD-FE. |
| SET-11 | 🟠 | About — Instagram embed web vs native (D-27 / About refresh AC) | `app/apps/mobile/src/features/about-landing/AboutInstagramEmbed.tsx:42-54` vs `.web.tsx:33-55` | About-refresh AC (2026-05-16, in spec header): "Instagram on web uses **no embedded browser module** — static card + external open only." Web variant is compliant. Native variant still renders `<WebView source={ABOUT_INSTAGRAM_EMBED_URL}>` (height 420, scrollable). The spec wording only forbids the embed on **web**, so native is fine — but anyone reading the AC might assume both surfaces dropped the embed. Surface for clarity. | Minor — implementation matches the letter of the AC; calling out so future readers don't accidentally "fix" the native path. | Spec footnote clarifying native-vs-web split. No TD. |
| SET-12 | 🟢 | About — language picker absent | (no implementation found) | Spec lists no language picker; memory notes Hebrew-only MVP. No locale toggle exists. Confirm: `i18n.changeLanguage` is not invoked from any settings surface — locale is fixed to `he` at startup. | Consistent with `R-MVP-Core-4` and project memory. No action needed; flagging for completeness since the audit task asked about it. | None. |
| SET-13 | 🟢 | About — broken/missing WhatsApp invite | `app/apps/mobile/src/features/about-landing/aboutExternalLinks.ts:11-16` | Both `ABOUT_WHATSAPP_GROUP_URL` and `ABOUT_WHATSAPP_PERSONAL_URL` use the SAME `wa.me/972528616878` direct-chat link (different prefilled text). The comment on line 11 says "replace with a stable `chat.whatsapp.com` invite when issued." Until then, the "community" link opens a 1-on-1 chat with the same personal number — not a community group. | UX bug (mis-labeled link). Low impact today but the user-visible label promises a group invite. | 🟢 BACKLOG or new TD-FE row. |
| SET-14 | 🟢 | About → Contact CTA target | `app/apps/mobile/src/features/about-landing/AboutContactLinks.tsx:68-75` | The bottom CTA routes to `/settings/report-issue`, which is `FR-MOD-002`. Spec FR-SETTINGS-008 also points to the same modal. Consistent; just noting the double entry point. | None. | None. |

---

## Cross-cutting observations

- **TD-53 (Edge Functions deploy)** — Still open. The smoke described in TD-53 ("edit a link, change `display_name` only, confirm same row id updates") is correctly enforced by the client+Edge contract (`useAddOrEditDonationLinkModal.ts:78-86` always passes `editingLink.id` as `link_id`; Edge function takes the UPDATE branch on non-empty `link_id`, `validate-donation-link/index.ts:136-177`). The contract itself is sound — but DON-1 (SSRF) is the bigger story behind the same edge function.
- **TD-155 (`categoryLabelHe` leak)** — Confirmed still present; touches `donations` only via Search results (`packages/domain/src/searchTypes.ts`). Out of scope for this audit area but adjacent to DON-4 — could be closed in the same PR.
- **TD-118 (delete-account V1.1)** — Confirmed gaps in SET-8. Bucket policy (TD-11) compounds the orphan-media risk.
- **Spec consistency check** — Multiple spec sections (FR-DONATE-003 AC2, FR-DONATE-001 AC1, FR-SETTINGS-001 AC1, FR-SETTINGS-002, FR-STATS-001 AC3, FR-STATS-004 AC2, FR-STATS-006 AC2) describe behavior the code no longer matches. Either the code or the spec should be brought into alignment before the next audit cycle, per `CLAUDE.md` §2 (Spec Validation Gate).

---

## Counts

- 🔴 HIGH: 2 (DON-1 SSRF · SET-1 legal links)
- 🟠 MED: 16
- 🟢 LOW: 5
- Total findings: 23 (cap was 25).
