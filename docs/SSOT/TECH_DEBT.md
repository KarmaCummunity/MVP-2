# Tech Debt Log — Karma Community MVP

| Field | Value |
| ----- | ----- |
| **Owner** | Engineering (auto-updated by agents) |
| **Last Updated** | 2026-06-15 (TD-81 closed — migration `0202` drops the `auth.uid() IS NOT NULL` guard from the `post_actor_identity_select_post_visible` RLS policy so guest (anon) visibility tracks `is_post_visible_to(p, auth.uid())` exactly — a guest reads an actor-identity row iff it can see the post (open+Public yes; OnlyMe/FollowersOnly/closed/removed no), matching the `media_assets` anon gate the audit cleared. Closes the latent guest-projection over-exposure (`applyPostActorIdentityProjection` no longer defaults to Public on an empty map). Verified on dev in a rolled-back tx (anon 1/0, stranger 1/0, owner 1/1); regression test `supabase/tests/0202_post_actor_identity_guest_visible.sql`. TD-173 part 5 closed — `GrantAdminRoleUseCase` (`admins.grant_role`) and `BanUserUseCase` (`reports.permanent_ban`) now enforce the `@kc/domain` `PERMISSION_MATRIX` via `hasPermission(getMyRoles(), …)` (defense-in-depth, FR-ADMIN-006): an unauthorized caller fails fast with a typed `forbidden` error instead of an opaque DB 42501. `getMyRoles()` added to `IModerationAdminRepository` + Supabase adapter (mirrors the `get_my_admin_roles` RPC); 3 new app-layer tests + 3 adapter tests; full `pnpm typecheck` + `@kc/application` (672) + moderation adapter (26) green. Also two stale-register cleanups: **removed TD-93** (admin universal-search — already shipped by migration `0149` `admin_search_users` / FR-ADMIN-019) and marked **TD-92 #1 already-correct** (`posts_expiry_transition` uses `created_at + interval '300 days' <= now()`, so a skipped pg_cron day self-heals — no change needed). Prior: 2026-06-14 (TD-87 closed — migration `0200` gates all six SECURITY DEFINER mutation RPCs (`close_post_with_recipient`, `reopen_post_marked`, `reopen_post_deleted_no_recipient`, `rpc_recipient_unmark_self`, `rpc_chat_set_anchor`, `rpc_submit_support_issue`) on `is_active_member(auth.uid())`, raising `account_not_active` (errcode `P0001`, since `42501` collides with `mapClosurePgError`'s `followers_only_requires_private`). Closes the gap where suspended/banned/pending users could still drive closure/reopen/recipient-unmark/chat-anchor/support mutations by calling the RPC directly (inconsistent with 0072's INSERT-active stance + 0183/D-57). Appeals unaffected — the account-blocked screen routes support via `mailto:`, not the RPC. Verified on dev in a rolled-back transaction; regression test `supabase/tests/0200_rpc_require_active_member.sql` (6 suspended-blocks + 3 active happy-paths). TD-172 closed — migration `0199` adds a `BEFORE UPDATE` guard trigger `posts_guard_client_status_transition` (SECURITY INVOKER, so `current_user` is the real role) that blocks client-direct `posts.status` transitions + `reopen_count` rewrites — closing the karma-mint (`→ closed_delivered`), fake-removal (`→ removed_admin`) and fake-expiry (`→ expired`) exploit from the 2026-06-14 security audit (finding M2). Discriminator: every privileged status writer runs as `postgres` and bypasses via `current_user not in ('authenticated','anon')` — `admin_remove_post`, `admin_restore_target`, `rpc_recipient_unmark_self`, the report-effects + `posts_expiry_transition` triggers/cron were already SECURITY DEFINER, and the three closure/reopen RPCs (`close_post_with_recipient`, `reopen_post_marked`, `reopen_post_deleted_no_recipient`) were converted INVOKER→DEFINER (each already asserts `auth.uid() = owner`). The only remaining client-direct transition is `open → deleted_no_recipient` (close-without-recipient). The DEFINER conversion also fixes a latent bug — `reopen_count` is absent from 0070's narrow client grant, so the INVOKER reopen RPCs raised `permission denied` on any DB with the grant correctly narrow (e.g. a fresh CI stack). `0199` also heals dev-DB catalog drift where `reopen_post_deleted_no_recipient` (created in 0070) was missing from the live `public` schema. Verified on dev in a rolled-back transaction (incl. a clean-stack narrow-grant simulation); regression test `supabase/tests/0199_posts_status_transition_guard.sql`. Prior: 2026-06-08 (TD-170 closed — `0163`/TD-163's `is_super_admin` column-grant revocation also broke five RLS policies (donation_links select/update/delete · ride_emergency_events_select · ride_ratings_select) that still tested super-admin via an inline `exists (… users … is_super_admin)` subquery; column privileges are checked at executor startup for every range-table entry, so every `authenticated` read 403'd (42501) — the rides-hub transport-links list among them. Migration `0193` swaps each subquery for the SECURITY DEFINER `has_admin_role(auth.uid(),'super_admin')` (0113/0116 lineage); regression test `supabase/tests/0193_rls_super_admin_reads.sql`. Prior: 2026-06-01 (db-deploy pipeline unblocked + admin-recognition regression closed — migration `0173` used non-idempotent DDL (`add column`/`add constraint`/index swap with no guards) and its effect had already been applied to the dev DB out-of-band while `schema_migrations` still read 0172, so every `supabase db push` since #465 died on `column scope_org_id already exists` (SQLSTATE 42701) and migrations `0173`–`0186` never reached dev; guarded 0173's column/constraint/index DDL with `if [not] exists` (functions were already create-or-replace) and reconciled the dev DB `0172`→`0186`. Process note: applying migrations out-of-band (not via the db-deploy workflow) is what wedged the ledger — avoid it. Separately, `useIsSuperAdmin` and `SupabaseModerationAdminRepository.isUserAdmin` still ran `.select('is_super_admin')` after `0163`/TD-163 revoked that column's client SELECT grant → 403 → the super_admin was silently treated as a normal user (no admin chrome / audit screen, ban-an-admin guard bypassed); both now resolve admin status via the SECURITY DEFINER `is_admin(uid)` RPC. TD-163 duplicate removed — a stale 🟠 `TD-163` row described the same H-BE-03 work already shipped in migration `0163` and recorded by the `✅ TD-163` row above; the leftover open row was deleted (no code change). TD-90 closed — `users_display_name_onboarding_check` (migration `0186`, `NOT VALID` so it applies cleanly yet enforces on every write) blocks nulling `display_name` after onboarding (NULL only valid while `onboarding_state = 'pending_basic_info'`); `city`/`city_name` stay nullable by design (D-25). TD-109 closed — post content edits now emit a `post_edited` audit_event via an AFTER UPDATE trigger (`posts_after_update_audit_edit`, migration `0185`; action allow-list extended v6→v7), so admins can see who edited what/when; status/visibility-only updates and system writes (no JWT) are excluded. Residual: a media-only edit isn't audited (the no-op title bump in `executePostUpdate` changes no content column). TD-83 closed — `find_or_create_support_chat` (0033 read-then-insert race) replaced by an atomic on-conflict upsert (migration `0184`). The fix surfaced a latent 42P10 bug shared with `rpc_get_or_create_support_thread` (0070): the bare `on conflict (participant_a, participant_b)` can't infer the PARTIAL `chats_unique_support_pair` index (0033 made it `where is_support_thread = true`), so both now carry the index predicate; idempotency SQL test covers both. TD-88 closed — `reports_insert_self` now also requires `is_active_member(auth.uid())` (migration `0183`, decision `D-57`) so suspended/banned users can't file retaliatory reports via the direct PostgREST path; SQL test `supabase/tests/0183_reports_insert_active_member.sql`. TD-119 + TD-3 closed — notification-tap routing now gated on auth readiness (`tapRouting.decideTapAction` / `isReadyToRoute`): a cold-start tap defers instead of racing AuthGate's `(auth)` redirect — restorable targets (`/chat/`, `/post/`, `/user/`) are handed to `redirectIntentStore` so AuthGate restores them after sign-in, others queue and flush once auth is ready; pure logic split into `tapRouting.ts` + `tapRouting.test.ts` (8 cases). TD-159 closed — `MessageBubble` tail-notch corners switched from physical `borderBottomLeft/RightRadius` to logical `borderBottomStartRadius`/`borderBottomEndRadius` so web (RN-Web `preprocess.js` maps these to CSS logical `border-end-start/end-radius`) and native (RN ≥ 0.71) mirror identically under RTL; verified via RN-Web source mapping + typecheck (live-chat screenshot not captured — dev test creds stale); residual `alignSelf` cross-axis parity folded into TD-110. TD-154 closed — Hebrew-literal guard `scripts/extract-hebrew-text.mjs` now wired into local `pnpm lint` via `lint:i18n` (matches the §6 pre-push gate) and covered by `scripts/extract-hebrew-text.test.mjs` (node:test, run in CI `hebrew-source-scan`); the CI `--check` gate pre-existed. TD-124 closed — source-code Hebrew literals (`aboutExternalLinks.ts`) already removed (scan is green) and `.json` is in scope; `app.json` native metadata stays Hebrew by design per D-24 / scanner skip. Prior: 2026-05-31 (loop housekeeping: TD-122 closed — `git rm --cached` the 36 committed `.turbo/cache` blobs (`.gitignore` already excluded `.turbo/`, only the index removal remained); TD-120 pruned — already closed by TD-165 (`useInfiniteQuery` + guarded `onEndReached`/`fetchNextPage` live in `(tabs)/index.tsx`); TD-139 closed — `postActorIdentity.test.ts` (24 cases over the D-26/D-31/D-39 truth table) + removed the `postActorIdentity.ts` coverage exclude (domain coverage 79.9%). Prior: 2026-05-29 (Wave 1 security audit: TD-161/162 registered; migrations `0161`–`0162`. Prior: 2026-05-28 TD-85 follow-up — migration `0160` `upsert_post_actor_identity` RPC fixes PostgREST upsert 403 after `0159` column grants; TD-85 closed — migration `0159` server-stamps `post_actor_identity.updated_at` + column-scoped UPDATE grants; TD-86 closed — `0158`; TD-84 closed — `0157`; P2.16 closed TD-71 — `0156`; P2.17 closed TD-70 — `0153`–`0155`). Prior: 2026-05-26 — PERF-3 Wave 2 partial (column pruning + RQ tuning + stats Realtime + feed gap-fill + actor identity projection completion); TD-135, TD-72, TD-126 closed; TD-98 narrowed. Prior: 2026-05-26 — PERF-2 Wave 1 (KCImage + transform URLs + memo sweep); TD-126 narrowed to useFeedRealtime issue only; TD-11 narrowed to CDN-only. Prior: 2026-05-26 — PERF-1 Wave 0 (Sentry crash + perf instrumentation); TD-125 closed. Prior: 2026-05-25 — TD-158 RTL platform-parity cleanup (rowDirectionStart helper, ~72 files migrated); spun TD-159, TD-160. Prior: 2026-05-22 — hotfix 0105 fixed TD-78 regression. Prior: 2026-05-17 — P2.14 closed TD-100/101/102/103, TD-66 downgraded; P2.22 closed TD-108; P2.13 closed TD-78 via 0093; TD-68 reopened, then hotfixed via 0095 and extended via 0098.)) |
| **How agents use this** | Before opening a PR, scan the area you're touching. Closing adjacent debt in the same PR is encouraged when scope is small. |

> Live execution state lives in [`BACKLOG.md`](./BACKLOG.md). Per-feature status lives in [`spec/*.md`](./spec/). This file is the active debt register.

**Severity**: 🔴 High · 🟠 Med · 🟢 Low
**ID lanes**: BE = `TD-50..99` · FE = `TD-100..149` (legacy IDs `TD-1..43` predate the lane split — keep as-is)

---

## Active

### Backend & data (domain · application · infrastructure-supabase · supabase/)

| ID | Sev | Item | Closing slice |
| -- | -- | ---- | ------- |
| TD-2 | 🟠 | FR-AUTH-006 AC1 breached-passwords check not implemented (Supabase doesn't expose HIBP OOTB; needs API integration) | P2.x hardening |
| TD-182 | 🟡 | **`.github/workflows/sync-main-to-dev.yml` (or equivalent "Sync main → dev" job) direct-pushes `main`'s tip onto `dev` after every squash promotion and is always rejected by `dev`'s branch protection** (`GH006: Protected branch update failed... Changes must be made through a pull request`) — confirmed on both PR #680's and PR #692's post-merge runs. Harmless today (a squash-merged promotion's tree already equals `dev`'s tree, so no actual drift results), but the workflow should either open a PR instead of pushing directly, or be removed if redundant. Separately, `main`'s branch protection has a `required_status_checks` context (`web export (production bundle)`) that only reported when `app/**` changed until fixed in PR #693 (job now always-runs, gates only its heavy steps) — watch for the same class of bug on any future required check added to `ci-*.yml`. | Opportunistic / CI cleanup |
| TD-183 | ✅ | **Closed — admin portal now renders via the active UI locale (FR-SETTINGS-018).** Rather than restructure the `admin*` locale modules' **function-valued label builders** into i18next `{{interpolation}}` templates (invasive, parity-test churn), added `src/i18n/useLocaleBundle.ts` — `useLocaleBundle()` (hook) / `localeBundle()` (non-hook) return the ACTIVE-language full bundle (same shape as the old `he` object, `en` widened via `unknown`; re-renders on `languageChanged`). All ~60 admin files (`app/(admin)/**` + `src/components/admin/**`) swapped `import he from '.../locales/he'` for the accessor (`he.x.y(args)` → `L.x.y(args)` — function builders preserved), with `typeof he.a.b` type positions → `LocaleBundle['a']['b']` and module-scope `he` aliases moved inside components / passed as params. Because `L` is typed as the bundle, **every key access is type-checked** (stronger than the user-facing `t('string')` surface). Added an ESLint `no-restricted-imports` guard (`app/eslint.config.mjs`) banning `**/i18n/locales/{he,en}` outside `src/i18n/**` to prevent regression (verified it fires). typecheck + 499 mobile tests + lint green. | Closed |
| TD-184 | 🟢 | **`app/(tabs)/search.tsx` search input hardcodes `textAlign="right"`** — the placeholder/typed text stays right-aligned in English (LTR) mode. Belongs to the FR-SETTINGS-018 RTL-polish family (TD-177); swap for a direction-aware alignment (e.g. `rtlTextAlignStart`) when sweeping remaining hardcoded `textAlign`/`right`/`left` in mobile. | With TD-177 RTL sweep |
| TD-64 | 🟢 | **`org_applications` → `organizations` not wired (FR-ADMIN-024).** Migration `0203` adds the `organizations` table but approving an `org_application` (migration `0168`, `admin_org_application_approve`) does not yet create an `organizations` row or a seed `org_admin` grant. Today orgs are created by direct DB insert / backfill. Wire the approval RPC to insert an org + initial `org_admin` grant. | Phase 3 / org onboarding |
| TD-53 | 🟠 | **Edge Functions deploy automation (post-merge verification).** Workflow `.github/workflows/supabase-functions-deploy.yml` runs on `push` to `main` when `supabase/functions/**` changes (plus `workflow_dispatch`). Uses GitHub Environment `supabase-prod` with `SUPABASE_ACCESS_TOKEN` + `SUPABASE_PROJECT_REF` (same as DB deploy). Runs `supabase functions deploy` (all functions) after `supabase link`. **Close this row** after the first successful Actions run on `main` + prod smoke: edit an existing donation link (change `display_name` only) and confirm the same row `id` updates — no new insert. | First green deploy + smoke |
| TD-55 | 🟡 | **FR-MOD-008 suspect-queue producers.** `moderation_queue_entries` table exists (0005) and `posts_after_reopen_check_queue` trigger emits on `reopen_count >= 5`. Missing: `forbidden_keyword` and `manual_flag` producers, and an admin-side surfacing as system messages (the table is currently silent — admins have no UI to see queue entries beyond the existing 3-reporter auto-removal alert). | Post-MVP |
| TD-56 | 🟡 | **FR-ADMIN-003 AC3 — 90-day re-registration block after permanent ban.** Currently a banned user can re-register with the same email/Google identity immediately. Spec calls for 90-day cooldown via a banned-identifiers lookup at signup. Requires DeletedIdentifier-style table + signup-path check. | Post-MVP |
| TD-59 | 🟢 | **Trigger-level visibility filter for `messages.system_payload` snapshots (reports).** UI-layer admin-gate is in place (`useIsSuperAdmin()` in `ReportReceivedBubble` / `AutoRemovedBubble`); a non-mobile client consuming the payloads would see private bios and content snippets. Add a visibility predicate to `reports_after_insert_apply_effects` that strips or omits `target_preview` for non-admin callers. Defer until a non-mobile client is on the roadmap. | Pre-EU-launch / non-mobile client |
| TD-60 | 🟢 | **RTBF scrub of `messages.system_payload` snapshots in `delete_account_data` (R-MVP-Privacy-6).** Migration `0046` snapshots `target_preview` (author handle + content snippet) into `messages.system_payload` at report time. `delete_account_data` (migration `0043`) does not null these snapshots. Defer until pre-EU-launch GDPR readiness pass. | Pre-EU-launch GDPR pass |
| TD-61 | 🟠 | **Rename `recipients` table → `respondents`.** Column `recipient_user_id` is semantically misleading: for `Request` posts the row stores the *giver*, not the receiver. Touches schema, RLS, RPCs (`profile_closed_posts`, `close_post_with_recipient`, `reopen_post_marked`), FE types (`Recipient` interface in `entities.ts`), and `FR-POST-017` CTA copy ("Remove my recipient mark"). Driven by D-19 (2026-05-13). | Post-MVP / standalone rename slice |
| TD-66 | 🟠 | **Deep-link manifest fingerprints still placeholder.** `apple-app-site-association` uses `REPLACE_WITH_APPLE_TEAM_ID`; `assetlinks.json` uses `REPLACE_WITH_ANDROID_RELEASE_SHA256`. Real values must be filled in before the universal link / app link works on a release build; until then native users see the chooser dialog instead of direct app open. **2026-05-17 (P2.14):** path coverage closed — AASA now covers `/auth/verify*`, `/auth/callback*`, `/post/*`, `/user/*`, `/chat/*`; `public/.well-known/README.md` documents how to fill the fingerprints from EAS release credentials. Downgraded from 🔴 to 🟠 because the launch-blocking path coverage gap is resolved; only the fingerprint substitution remains. | Pre-launch (EAS release build) |
| TD-67 | 🟢 | **`cities.lat/lon` remainder after `0089_cities_geo_bulk_backfill`.** Fifteen CBS codes still have `NULL` coordinates (listed in the migration header): no confident match in `israel-geolocation` + OSM Nominatim within the `0021` bounding-box checks. Distance sort correctly degrades for posts in those settlements (`FR-FEED-006` AC3). Close by joining CBS GIS centroid export (or another authoritative point layer) keyed by `סמל_ישוב`. | Opportunistic / data refresh |
| TD-118 | 🟠 | **Delete Account V1 deferred polish — orphan storage + cooldown + email + recovery window.** P2.2 V1 shipped immediate hard-delete. Remaining items for V1.1: (1) orphan `post-images` cleanup cron — Edge Function logs storage delete failures but does not retry; orphan media in this public-read bucket remains world-readable until cleaned. (2) `DeletedIdentifier` cooldown table (FR-AUTH-016) — 30-day reuse lockout for email/Google identity. (3) Confirmation email (FR-NOTIF-012) — requires email infra. (4) Recovery/undo window — current V1 is irreversible; industry baseline is 30 days. | V1.1 |
| TD-69 | ✅ | **Closed — dev migration-ledger drift guard.** Agents kept applying dev schema via the Supabase MCP `apply_migration` tool (records a TIMESTAMP version, e.g. `20260701130827`) instead of `supabase db push` from a committed sequential file, so every out-of-band apply silently broke the next `DB deploy` on `dev` ("Remote migration versions not found in local migrations directory") until a manual `supabase migration repair`. Fixed by (1) convention + enforcement in CLAUDE.md §9 + `OPERATOR_RUNBOOK.md` → "Migration application rules"; (2) CI guard `.github/workflows/ci-migration-drift.yml` (`CI — migration drift`) that fails on any remote version with no local file (scheduled + push-to-dev + manual; non-required, never gates a PR); (3) helper `scripts/reconcile-migration-drift.mjs` emitting the exact repair commands. Detector `scripts/check-migration-drift.mjs` (+ `.test.mjs`, 8 cases). Verified live against the dev ledger's real `20260701130827` orphan. | Closed |
| TD-171 | 🟢 | **Survey seeds are super-admin-gated and silently skip.** `0131_public_research_responses.sql` (Survey B `alt-platforms-research`) and `0130_surveys_and_feedback.sql` (Survey A `ux-experience`) call `publish_survey_version` only `if` a super-admin row already exists at apply time, else `raise notice '… skipping …'; return`. `publish_survey_version` itself needs ≥1 user because `survey_versions.published_by` is `NOT NULL → auth.users`. A fresh prod/CI apply with no super-admin therefore leaves the survey unpublished, which silently 400s every public submit (`survey_not_found`) and errors the anon `get_public_research_questions` load — i.e. the whole public-research feature is dark with no migration error. Prod is currently seeded (verified 2026-06-14 via live `get_public_research_questions` + a real submit returning a `responseId`), so this is latent, not active. Fix: make the seed idempotent and self-sufficient — allow a nullable/system `published_by`, or re-assert the publish in a later guarded migration / post-deploy step keyed on `surveys.current_version = 0`. | Pre-launch hardening |

<!-- ============================================================ -->
<!-- Audit 2026-05-16 — BE rows (TD-67..99)                       -->
<!-- Source: docs/SSOT/audit/2026-05-16/99_consolidated.md          -->
<!-- ============================================================ -->

| TD-71 | ✅ | **Closed by migration `0156_recipient_unmark_no_owner_decrement.sql`.** `rpc_recipient_unmark_self` no longer manually decrements owner counters; `reopen_post_deleted_no_recipient` remains the single decrement source per the `0045` recompute model. | Closed |
| TD-73 | ✅ | **Closed by PR #415 (migration `0134_fix_push_notification_routing.sql`).** Follow triggers now lookup `share_handle` before emitting the `handle` param; mobile `/user/[handle]` route falls back to `findById` for UUID-shaped params so notifications already enqueued before the migration also resolve. | Closed |
| TD-74 | ✅ | **Closed by PR #415 (migration `0134_fix_push_notification_routing.sql`).** `messages_enqueue_notification` now iterates over both participants for system messages; per-recipient dedupe key (`system:<message_id>:<recipient_id>`) bypasses the global unique index that would silently no-op the second call. `enqueue_notification`'s self-suppression catches the actor. | Closed |
| TD-76 | ✅ | **Closed by migration `0135_users_enqueue_account_suspended.sql`.** New trigger `users_enqueue_account_suspended` fires on every transition into `suspended_admin` or `banned` and enqueues a Critical `account_suspended` notification with `bypass_preferences: true`. Routes to `/account-blocked`. Dedupe key includes status so a restore-then-re-suspend re-notifies. | Closed |
| TD-77 | ✅ | **Closed by migration `0136_chats_enqueue_auto_removed.sql`.** New trigger `chats_enqueue_auto_removed` fires on `NULL → non-NULL` transition of `chats.removed_at` and enqueues per-participant Critical `chat_auto_removed` notifications. Both parties learn their conversation was removed even though the chat itself is no longer visible. A future FE PR can add the in-app banner UI; the BE silent-vanish defect is closed. | Closed |
| TD-80 | ✅ | **Closed by P2.18 (FR-SETTINGS-010 server-driven legal docs).** Replaced the original "in-app web views with remote-config URLs" plan with native Markdown rendered from `legal_documents` + `legal_document_versions` (migration `0108`). AC3 satisfied via `needs_legal_reacknowledgement` + per-version `user_legal_acceptances` log. | Closed |
| TD-81 | ✅ | **Closed (migration `0202`).** Dropped the `auth.uid() IS NOT NULL` prefix from `post_actor_identity_select_post_visible`; visibility is now governed purely by `is_post_visible_to(p, auth.uid())` — the canonical gate, which already returns true for a NULL viewer ONLY on guest-visible posts (open + Public). A guest therefore reads an actor-identity row exactly when it can see the post (same gate `media_assets` uses for anon, audit-cleared 2026-06-14), so `applyPostActorIdentityProjection` resolves correctly for guests with no over-exposure of OnlyMe/FollowersOnly/closed identities. Regression test `supabase/tests/0202_post_actor_identity_guest_visible.sql` (anon 1/0, stranger 1/0, owner 1/1). | Closed |
| TD-161 | ✅ | **Closed (audit 2026-05-29 Wave 1).** Migration `0161` revokes anon EXECUTE on `submit_public_research_response` and `has_admin_role`. Follow-up: server-side IP hash only (Edge). | Closed |
| TD-162 | ✅ | **Closed (audit 2026-05-29 Wave 1).** Migration `0162` — `rate_limit_buckets`, message/report triggers (NFR-SEC-009). | Closed |
| TD-163 | ✅ | **Closed (audit 2026-05-29 Wave 2).** Migration `0163` — column grants + `users_public` view + `users_get_self_private_fields`; adapter scrub + follow embed pruning. | Closed |
| TD-87 | ✅ | **Closed (migration `0200`).** All six SECURITY DEFINER mutation RPCs now gate on `is_active_member(auth.uid())` and raise `account_not_active` (errcode `P0001`, not `42501` — which `mapClosurePgError` maps to `followers_only_requires_private`): `close_post_with_recipient`, `reopen_post_marked`, `reopen_post_deleted_no_recipient`, `rpc_recipient_unmark_self`, `rpc_chat_set_anchor`, `rpc_submit_support_issue`. Consistent with 0072's INSERT-active stance + 0183/D-57. Appeals are unaffected — the account-blocked screen routes support via `mailto:`, not `rpc_submit_support_issue`. Regression test `supabase/tests/0200_rpc_require_active_member.sql` (6 suspended-blocks + 3 active happy-paths). | Closed |
| TD-68 | 🟠 | **Six SECURITY DEFINER predicates callable as direct PostgREST RPCs (reopened by 0095, extended by 0098, anon spillover closed by 0099).** Same enumeration class: `is_active_member` (revoked by 0090 §1, restored by 0095), and `is_admin` / `is_blocked` / `is_following` / `has_blocked` / `active_posts_count_for_viewer` (revoked by 0070 §1.2 + §15.2, restored to `authenticated` by 0098 after production users/posts/chats SELECT started 403-ing on 2026-05-17, and `is_blocked` further restored to `anon` by 0099 after the guest-preview surface 401-ed via `users_select_active` USING on 2026-05-18 — the dev project had retained pre-0070 grants on both anon + authenticated, masking the regression for both roles). Same root-cause as 0095 documents: RLS policy expressions evaluate as the calling role, so EXECUTE is required even when the function is SECURITY DEFINER. Proper fix: either (a) inline the predicate bodies into each calling RLS expression and DROP the functions, (b) add a `current_setting('request.path', true) IS NULL` guard inside each function body that aborts direct PostgREST RPC invocation while permitting RLS / SECURITY-DEFINER callers, or (c) move all six predicates into a non-public schema (e.g. `internal_`) and grant USAGE only to `postgres`. Low severity — user_ids are already inferable from feed/profile reads and the booleans disclose only block/follow/admin edges and an open-post count. | Pre-launch hardening |
| TD-89 | 🟢 | **`users_biography_no_url_check` constraint `NOT VALID` + over-matches innocent text.** Legacy URL-containing bios stay; regex `[\w.-]+\.[a-z]{2,}` false-positives `e.g.`, `Dr.Smith`, `Node.js`, email-shaped tokens. Either validate + tighten regex (use a curated shortener list) or downgrade to FE-only soft-warn. | Opportunistic |
| TD-91 | 🟢 | **Migration convention sweep.** Three patterns: (a) audit-action CHECK rename dance (0075) silently drops actions if the next migration forgets to copy the full list — move to enum or reference table; (b) `create or replace function` without `drop function if exists` cycle (§1.10 family) — 0072, 0083, 0084 inherit; (c) `dispatch-notification` `resolveString` returns raw key on miss — add `console.warn` + CI check that every `enqueue_notification(..., key, ...)` has an i18n entry. | Convention |
| TD-92 | 🟠 | **Notifications dispatcher / outbox hardening cluster.** (1) ✅ **Already correct (verified 2026-06-15)** — `posts_expiry_transition` expires `where status='open' and created_at + interval '300 days' <= now()` (catch-all, not `= day 300`), so a skipped pg_cron day self-heals on the next run; no change needed. (2) Per-minute chat dedupe key (`chat:<chat>:<sender>:<minute>`) blocks the dispatcher's coalescer so AC3 "X new messages" count never engages. (3) `last_seen_at` refresh only on `RegisterDeviceUseCase.upsert` — long-running app sessions get pruned at 90d mid-session; add daily AppState=active touchpoint. (4) Outbox TTL of 7d releases dedupe constraint for kinds that need lifetime uniqueness (`auto_removed`, `unmark_recipient`) — add a permanent dedupe table. | Pre-launch hardening || TD-94 | 🟠 | **Moderation / admin sweep.** (1) `rpc_submit_support_issue` (0074) doesn't write to `reports` even though FR-MOD-002 Related says `Domain: Report (target_type=none)` — now documented as intentional by `D-41` (support tickets and moderation reports stay separate surfaces). (2) ✅ **Closed by pre-A2 hardening (migration 0129)** — `reports_validate_before_insert` now raises SQLSTATE `P0020` with `report_target_already_moderated` for posts hidden / users banned / chat messages deleted, so the mobile SDK can surface a distinct toast instead of the legacy `report_target_not_visible` masking. (3) `is_post_visible_to` has no admin bypass → admin feed-rank / closure / batched joins skip moderated posts. (4) ✅ **Closed by FR-ADMIN-013 AC3 (P3.A1, migration 0119)** — `admin_restore_target` rewritten in migration 0119 to dismiss only the cases on the specific target being restored; it no longer dismisses other open reports on the same target. Verified by `reportsRpc.integration.test.ts` TD-94 case (3 reporters → auto-remove → dismiss one → target restored, other two reports stay `open`). (5) ✅ **Closed by pre-A2 hardening (migration 0128)** — `admin_restore_target` audit entries now carry the same `{distinct_reporters}` JSON metadata as the matching `auto_removed_target` event, so the moderation timeline reads symmetrically. (6) ✅ **Closed by pre-A2 hardening (migration 0125)** — `reports_threshold_window_days` introduces a 14-day freshness window: only reports filed within the window count toward the 3-distinct auto-removal threshold, so year-old open rows can no longer tip the threshold. | Pre-launch hardening |
| TD-95 | ✅ | **Closed by P3.A0 (FR-ADMIN-010) — single-active-super_admin enforced at DB via partial unique index on `admin_role_grants`.** Migration `0112_admin_role_grants_table.sql` adds `unique index admin_role_grants_single_super_admin_uniq on admin_role_grants (role) where role = 'super_admin' and revoked_at is null`, satisfying FR-ADMIN-006 AC2 at the database layer (no application code can elevate a second super_admin without the INSERT failing). Remaining cluster items (Follow pagination cursor mismatch, accept/reject/cancel race, 42501 → "blocked" mis-mapping, audit `metadata` schema drift, `auto_removed` bubble only injected into 3rd reporter's thread, `admin_restore_target` clobbers prior `pending_verification` status) are tracked under P3.A1..A2 closure work and audit follow-ups. | Closed |
| TD-96 | 🟢 | **Chat surface BE cleanup.** (1) `post_closed` system bubble not registered in `SystemMessageBubble` — blank pill once TD-148 lands. (2) Three layered `rpc_get_or_create_support_thread` definitions (0011, 0033, 0070) — collapse to one. (3) `dispatch-notification/i18n.json` no missing-key fallback — surface a `console.warn` and add CI check. | Opportunistic |
| TD-97 | 🟠 | **Donations BE cleanup.** Missing jgive CTA (FR-DONATE-003 AC2); grid layout vs vertical AC1 + missing divider between modalities and categories (FR-DONATE-006); dead `softHide` repository method post-0050 purge; spec-divergent reachability rule (code accepts `!= 404`, spec says `200..399`); error-code parity gaps (`server_error` not in client union, `invalid_input` not in spec); rate-limit defense thin; edge-function HTTP 200 + body-code on errors inconsistent with `delete-account` / `dispatch-notification`; vestigial `.is('hidden_at', null)` filter. | P2.x / opportunistic |
| TD-98 | 🟠 | **Statistics — remaining spec mismatches (Wave 2 G1 closed AC2 reactive + AC4 cached-endpoint via Realtime; sub-bullets 1+2 resolved; FR-KARMA-009 closes the focus-only AC2 gap via persistent `postgres_changes` own-row subscription in `useMeRealtime`).** Remaining open items: (3) FR-STATS-003 AC1 caps at 30; use case allows 50. (4) FR-STATS-002 AC4 says clamp triggers an alert (NFR-RELI-005); code only `RAISE NOTICE`. (5) FR-STATS-006 AC2 says auth-only, view granted to `anon` for FR-FEED-014 — spec contradiction. (6) Stats screen renders community panel first, spec says counters-then-timeline-then-community. | Pre-launch hardening |
| TD-99 | 🟠 | **Settings — Account/Notifications/Legal cluster.** Section order: Legal as its own section, currently nested under Support; FR-SETTINGS-006 toggles spec'd inline but live in sub-screen; FR-SETTINGS-002 Account section unbuilt (no read-only display_name/email/phone, no auth-method label, no password-change CTA); FR-SETTINGS-013 read-only contact-change modal absent; ~~FR-SETTINGS-009 mailto + `aboutExternalLinks.ts` use `support@karma.community` (un-owned domain)~~ ✅ **closed 2026-06-09 (`D-59`)** — the dead `support@karma.community` row was removed from About → Contact; single operated mailbox is `karmacommunity2.0@gmail.com`; FR-SETTINGS-009 AC1 amended; FR-SETTINGS-011 AC1 logout has no confirmation modal; FR-SETTINGS-012 AC1 delete-account uses keyword instead of display_name; SET-8 — re-evaluate TD-118 severity given public-read bucket orphan risk. | Pre-launch hardening |
| TD-51 | 🟡 | **Wire `consent_check_skipped_offline` events to a server-side `legal_events` log table (FR-SETTINGS-010 AC6 hardening).** Today the gate's fall-open path only `console.warn`s, which is invisible in an ILITA audit. Add a server-side log table + Edge Function endpoint so the bypass is durably recorded. Required within one sprint of FR-SETTINGS-010 ship to defend the offline-fall-open posture. | Pre-launch hardening |
| TD-52 | 🟡 | **Two-person approval workflow for `publish_legal_document`.** A second `super_admin` must approve before `effective_date` arrives. Reduces the blast radius of a single admin pushing bad legal copy. Audit-readiness hardening on top of FR-SETTINGS-010. | Pre-launch hardening |
| TD-54 | 🟡 | **Periodic check + alert when the ILITA database-registration threshold (תיקון 13 §17B) is approached.** Counts active users + sensitive-data fields; pages when within X% of the trigger. Avoids the "we crossed the line and didn't know" failure mode. | Pre-launch hardening |
| TD-167 | ✅ | **Closed (2026-06-08).** `feed_ranked_ids` had three coexisting overloads (13-arg `0022`, 14-arg `0025`, 15-arg `0126`) because `0025`/`0126` added params via `create or replace` with a changed signature without dropping the prior one — a latent PostgREST `PGRST203` ("could not choose the best candidate") hazard confirmed on the live dev DB. Migration `0187` drops the 13/14-arg overloads (idempotent). | Closed |
| TD-168 | ✅ | **Closed (2026-06-08).** Root `.mcp.json` pointed the Supabase MCP at the **prod** project (`slxijdfvinbjmrsfgbzx`) instead of dev (`roeefqpdbftlndzsvhfj`), contradicting `app/.mcp.json` + `ENVIRONMENTS.md` and dev-only authority. Repointed to dev. | Closed |
| TD-170 | ✅ | **Closed (2026-06-08).** Same root cause as TD-163: migration `0163` revoked the `is_super_admin` column SELECT grant from `authenticated`, but five RLS policies still tested super-admin via an inline `exists (select 1 from public.users u where u.user_id = auth.uid() and u.is_super_admin = true)` subquery — `donation_links` select/update/delete (`0014`), `ride_emergency_events_select` (`0178`), `ride_ratings_select` (`0179`). Postgres checks column privileges at executor startup for every range-table entry in a plan, so any `authenticated` read of those tables raised `permission denied for table users` (SQLSTATE 42501 → HTTP 403) regardless of OR short-circuiting; the rides-hub transport-links list (`DonationLinksList categorySlug="transport"`) 403'd for every signed-in user. Anon was unaffected (policies are `to authenticated`). Fix mirrors the client-side `is_super_admin` selects already moved to the `is_admin(uid)` RPC: migration `0193` swaps each inline subquery for the SECURITY DEFINER helper `public.has_admin_role(auth.uid(), 'super_admin')` (added `0113`, precedent `0116`). Regression test `supabase/tests/0193_rls_super_admin_reads.sql` runs as `authenticated` and asserts the reads succeed. | Closed |
| TD-57 | 🟢 | **Multi-language support for legal documents.** Schema already carries `legal_document_versions.language` (default `'he'`); `LoadLegalDocumentUseCase` would need locale-aware variant selection. Deferred per `R-MVP-Core-4` (Hebrew-only MVP). | Post-MVP |

### Mobile & UI (apps/mobile · packages/ui)

| ID | Sev | Item | Closing slice |
| -- | -- | ---- | ------- |
| TD-176 | 🟠 | **English UI bundle is machine-translated (FR-SETTINGS-018).** The `apps/mobile/src/i18n/locales/en/**` tree (~3,500 strings) was auto-translated from Hebrew and needs a native-English review pass for tone, idiom, and truncation (buttons/labels). A `localeParity` test guards key structure, but not wording quality. Translation subagents flagged specific judgment calls (unit order, gendered-slash collapses, `MA"B`/gemach glosses) in the PR body. | Post-launch polish |
| TD-177 | 🟠 | **Residual hardcoded RTL not direction-aware (FR-SETTINGS-018).** The centralized helpers (`rtlTextAlignStart`, `rowDirectionStart`, `webTextRtl`, `textAlignStart()`, `layoutWritingDirectionStyle()`) now follow the active language, but ~30 scattered `writingDirection: 'rtl'` hardcodes remain in long-form content (legal readers/markdown, survey field styles, a few modals/PostCard). In English these areas render RTL-biased. Migrate each to `layoutWritingDirectionStyle()` / `isLayoutRtl()`. | Post-launch polish |
| TD-178 | 🟡 | **Native language switch relies on `DevSettings.reload` (FR-SETTINGS-018).** Web switches via `window.location.reload()`. Native has no `expo-updates`, so `reloadApp()` falls back to `DevSettings.reload()` (dev only) — a production native build won't reload to apply the RTL/LTR flip. Add `expo-updates` + `Updates.reloadAsync()` (or a DB-backed preference applied at next cold start). Low urgency while web is the active target. | When native ships |
| TD-179 | 🟡 | **GloWe admin page still carries demo-era localStorage panels.** `pages/admin.html` "join requests" (`getAdminReviewUsers`) and "hidden items" (`getHiddenModerationItems`) render from localStorage stores that are always empty in production (real queues are the server-backed pending-orgs + reports panels). Remove the panels + their helpers (`updateUserModerationStatus`, `hideReportedItem`, `restoreModeratedItem`) and drop the dead stats. | Opportunistic |
| TD-180 | 🟡 | **GloWe chat thread has no realtime subscription.** `renderChatThread` loads once and re-renders on send; a counterpart's message appears only on re-open/refresh, and the header unread badge (`refreshMessagesBadge`) computes once per page load. KC's `supabase_realtime` publication already covers `messages` — subscribe per open thread + refresh the badge on insert. | Post-launch polish |
| TD-181 | 🟢 | **GloWe full-flow E2E accumulates chat messages on dev.** `glowe-full-flow.spec.ts` sends stamped messages between seeded personas each run; `public.messages` has no client DELETE policy so the spec cannot clean up after itself (created events/offers ARE cleaned via owner delete). Accepted on dev; if noise grows, add a service-role cleanup step to the seed workflow. | When noisy |
| TD-169 | ✅ | **Closed (2026-06-08).** `useFeedRealtime` read the Home Feed cache as a bare `FeedPage` (`cached.posts[0]`) but the feed is a `useInfiniteQuery` → cache is `InfiniteData<FeedPage>` (`{ pages }`). `cached.posts` is `undefined`, so `cached.posts[0]` threw `Cannot read properties of undefined (reading '0')` → app error boundary ("משהו השתבש"). Latent since the feed moved to infinite query (TD-165); surfaced by PERF-6 (#494) which restores a persisted cache so `cached` is defined on the first cold-start render. Fixed: optional-chain via `cached?.pages?.[0]?.posts?.[0]`; `performGapFill` now prepends into `pages[0].posts` (dedupe across pages). | Closed |
| TD-24 | 🟠 | **Apple SSO implemented in code (`D-58`, 2026-06-04)** — native Sign in with Apple shipped (`SignInWithAppleUseCase` + `signInWithIdToken`); the iOS button now performs real Apple auth instead of routing to email. **Go-live pends Supabase Apple provider config (Service ID + key) + a native rebuild** (the entitlement/plugin are native; OTA cannot add them). Remaining debt: Phone OTP button still a placeholder (Israeli SMS path) — folded into **`TD-151`**. **2026-05-12:** misleading full-width Phone / Email CTAs removed from welcome (`(auth)/index.tsx`). | P3.3 |
| TD-29 | 🟠 | **`pnpm lint:arch` file-size allowlist.** `FILE_SIZE_ALLOWLIST` in `app/scripts/check-architecture.mjs` still tracks files over the 300-LOC cap (e.g. `(tabs)/search.tsx`, `edit-post/[id].tsx`). Goal is to split implementations until each entry can be deleted. **2026-06-29:** `packages/application/src/index.ts` entry removed — shrunk to 300 LOC by collapsing multi-line port re-export blocks (during FR-TRANSLATE-002). | Opportunistic |
| TD-58 | ✅ | **Closed (2026-06-29).** Migration `0210` (Phase 1c) replaces the deny-by-default `content_translations_no_direct_read` policy with the narrow `content_translations_select_visible_post` SELECT policy (authenticated readers see a translation row only when it belongs to a post, reusing the posts `is_post_visible_to` RLS via an `EXISTS` subquery) and adds the SECURITY INVOKER `get_post_translations(uuid[], text)` read RPC (joins through `posts`, so RLS filters visibility before any row returns; hits only). Message (chat) translations stay unreadable — Phase 3. Regression test `supabase/tests/0210_content_translations_read.sql` (reader sees 2 public-post rows, 0 for an OnlyMe post, 0 for chat, 0 for an un-cached language). | Closed |
| TD-75 | 🟠 | **UNBLOCKED (2026-07-04): switched the default provider to a keyless free service, so translation no longer depends on any Gemini key/quota.** Root cause of the earlier outage: the `GEMINI_API_KEY` Google project returned HTTP 429 `"generate_content_free_tier_requests, limit: 0"` — the free tier is **not enabled on that project** (project-wide, all Flash models), and the PM directive is "free models only, no billing". Fix: added `GoogleFreeProvider` (`_shared/translation/googlefree.ts`) using Google's public keyless `translate_a/single` endpoint behind the existing `selectProvider` seam, and made it the default (`TRANSLATION_PROVIDER` unset ⇒ `googlefree`; `gemini` stays opt-in via env). Auto-detects source (normalising `iw`→`he` so the same-language short-circuit still fires). Verified live on dev: EN→HE `translated`, repeat `cached`, HE→HE `skipped`. **Remaining debt (why this is 🟠 not ✅):** (1) the endpoint is undocumented / ToS-gray and may change without notice — before GA, move to an official free-or-DPA provider; (2) the original privacy goal still stands — **for real users, translation should run on a zero-retention/DPA model (D-63/D-65)**, which the keyless public endpoint is NOT. The provider seam makes both a code/env swap. | Opportunistic (pre-GA: provider hardening) |
| TD-79 | 🟢 | **`translate` Edge Function has no automated tests.** The repo has no Deno test harness; the function was verified manually via `supabase functions serve` + curl. Add Deno tests (or extract pure helpers into a testable module) once a harness exists. | Opportunistic |
| TD-82 | 🟡 | **UGC translation cost controls deferred from Phase 1b.** Per-user rate limit + daily spend ceiling (design §7) and language-batching of pending misses (§5) are not implemented in 1b (free tier + single-target only; input-length cap is in place). Revisit with the posts read path (Phase 1c) and before switching to the paid tier. | Phase 1c / before paid tier |
| TD-62 | 🟢 | **FR-NOTIF-004 (message_undeliverable) — deferred from P1.5 PR-1.** Requires sender-side detection of recipient account state change. Surface via a daily cron that scans outgoing messages where the recipient's `account_status` flipped to suspended/deleted within 24h. | Post-MVP |
| TD-63 | 🟢 | **Notification icon placeholder.** `app/apps/mobile/assets/notification-icon.png` is a 96×96 placeholder. Replace with a designed icon (white shape on transparent, Android adaptive guidelines). | Pre-launch |
| TD-65 | 🟠 | **Web Push parity deferred from P1.5.** Implement VAPID + Service Worker; outbox + dispatcher pattern unchanged, only the adapter changes (Web Push HTTP API instead of Expo Push). | Post-MVP |
| TD-123 | 🟢 | **Closure telemetry events.** FR-CLOSURE-002 AC3 specifies `closure_step1_completed`; the FR analytics map mentions further closure events. No telemetry infra exists in the repo yet. Defer until analytics pipeline lands. | Analytics |
| TD-128 | 🟠 | **SupabaseUserRepository.ts file size.** Currently allowlisted at max:270 (was 215 → bumped during P1.1). The follow surface is now ~50% of the file. Future ergonomic move is a composed `SupabaseUserFollowFacade` that the main repo defers to — but until other ports (auth, onboarding, blocks) are also done, the split would be premature. | P2.4 |
| TD-133 | 🟠 | **Search-mechanism files exceed 200-LOC cap.** Remaining over-cap files in `apps/mobile/app/(tabs)/search.tsx` (~634), and the still-allowlisted `SearchResultCard.tsx` / `SearchFilterSheet.tsx` barrels after their splits. Continue extracting until allowlist entries drop. P1.2 did not bundle this split because consolidating `SearchFilterSheet` with the new `<PostFilterSheet>` is a non-trivial UX migration tracked separately under TD-136. | Pre-launch hardening |
| TD-134 | 🟠 | **Analytics spine for `feed_fallback_applied` and other events.** FR-FEED-007 originally specified an analytics event for the cold-start fallback; FR-FEED-007 is now deprecated but the broader observation stands — there is no `IAnalytics` port nor any wiring. When P1.6 (community stats / observability) lands, introduce `@kc/application/analytics/IAnalytics` and instrument the discovery surface (filter applied, sort changed, new-posts pill tapped, nudge dismissed). | P1.6 |
| TD-136 | 🟠 | **Universal Search tab does not yet expose the new post-filter dimensions.** FR-FEED-018 promises a shared post-filter vocabulary between the Home Feed and the Search tab, but the Search tab's `SearchFilterSheet` still reads from `searchStore` only (no itemConditions, no radius slider, no 3-mode status). When TD-133 splits the search files, fold in the new dimensions so the user sees identical filter sets across both surfaces. | Pre-launch hardening (depends on TD-133) |
| TD-137 | 🟠 | **`feed_ranked_ids` RPC is N+1 against `posts` on the adapter side.** `SupabasePostRepository.getFeed`'s distance-aware path fires the RPC for ordered IDs + distance and then a second `SELECT … IN(...)` for full PostWithOwner rows. Two round-trips per page is acceptable at MVP scale (~50 KB returned) but at scale the second query duplicates RLS work. Push the full row fetch into the RPC return type via composite types or JSON aggregation. | Pre-launch hardening |
| TD-148 | 🟠 | **System messages from the closure trigger are hardcoded Hebrew.** `supabase/migrations/0031_post_closure_emit_system_messages.sql` inserts `'הפוסט סומן כנמסר ✓ · תודה!'`, `'הפוסט נמסר למשתמש אחר'`, and `'המפרסם סגר את הפוסט — הפריט לא נמסר'` directly. Acceptable while the app is Hebrew-only. When i18n infra lands for system messages, move strings to a translation table (or to an Edge Function that resolves the recipient's locale) and emit a structured `system_payload` only — the UI then renders the localized body. | When i18n infra lands |
| TD-150 | 🟡 | **No RNTL/jest infrastructure in `@kc/mobile`.** P1.3+P2.2 slice planned UI tests for `BanUserModal`, `AutoRemovedBubble`, `account-blocked` (Task 26.5 in `docs/superpowers/plans/2026-05-12-moderation-admin-actions.md`) but mobile has no jest preset wired (`package.json` only exposes `start`/`typecheck`). Setting up `jest-expo` + `@testing-library/react-native` + `@testing-library/jest-native` + transform config is a non-trivial standalone slice. Application-layer use cases ARE covered by vitest. **2026-05-25:** `sonar-project.properties` now excludes `apps/mobile/app/**`, `src/components/**`, `src/features/**`, `src/hooks/**`, `src/lib/**`, and `*Composition.ts` from the new-code coverage gate so the SonarCloud check reflects testable scope (`packages/**` + `apps/mobile/src/store/**` + non-composition `services/**`). `src/lib/**` joined the list after the dev→main release PR repeatedly stalled at 77.5% coverage — almost the entire `lib/` surface is React-adjacent (RTL platform helpers, query-key invalidators, type mappers consumed by RN components) and has no isolated runner today. When TD-150 closes and a first UI test wave lands, narrow those `sonar.coverage.exclusions` lines back to demo-only paths. | Standalone test-infra slice |
| TD-164 | ✅ | **Closed (audit 2026-05-29 Wave 3).** `performFullSignOut()` — legal exit, account gate, restore expiry, settings; fail-closed legal/onboarding gates. | Closed |
| TD-165 | ✅ | **Closed (audit 2026-05-29 Wave 7).** Home feed `useInfiniteQuery` + `onEndReached`; `userProfileKeys` for nudge query. | Closed |
| TD-151 | 🟠 | **`FR-AUTH-002` welcome parity — Phone OTP + primary Email entry + real Apple.** The welcome screen must again expose distinct methods per AC1–AC2: SMS OTP (`FR-AUTH-005`/`007`), first-class email/password + sign-up entry (not only a tertiary link), and native Apple (`FR-AUTH-004`). Today: Google + guest + email link + iOS Apple stub → `/(auth)/sign-in`. | P3.2 / auth hardening |
| TD-152 | 🟢 | **Pre-migration `report_received` rows render as title-only (degraded mode).** Migration 0040's trigger wrote `body=null` for system messages; rows created before migration 0046 have `system_payload.target_preview = null`. These render as title-only ("Spam · post") in `ReportReceivedBubble`. A backfill of `system_payload` from `reports` + target tables would restore full preview for legacy rows. Cosmetic — no data is lost. | Opportunistic |
| TD-156 | 🟡 | **`hide_from_counterparty` on feed + universal search.** Post detail + profile closed-posts now mask third parties when `hide_from_counterparty` or `surface_visibility = OnlyMe` (2026-05-24, `D-39`). Feed ranked path and universal search post cards still omit listing-host context for partner-surface mask — low priority until those surfaces show participant chrome. | FR-POST-021 / TD-72 cluster |
| TD-157 | 🟢 | **Shell primitives use substring path matching — fragile before going live.** `NavigationRail.isTabActive` and `AppShell.useDetectedVariant` both match routes via `pathname.includes('/profile')` / `joined.includes('settings')`. Works today because there's only the dev preview route (`/dev/shell-preview`) and 5 distinct tab routes, but any future route containing these substrings (e.g., `/user/profile-settings`, `/post/settings-discussion`) would falsely match. Replace with segment-array equality via `useSegments()` — mirror the approach `TabBar.tsx` already uses. **Must close before PR 2 (FR-RESP-002) wires the shell into root `_layout.tsx`.** **Closed in FR-RESP-002.** | FR-RESP-002 blocker |
| TD-158 | 🟢 | **RTL platform-parity cleanup — `flexDirection`/`textAlign` double-flip on native (resolved 2026-05-25).** Background: app forces RTL on both platforms (`_layout.tsx` web sets `document.documentElement.dir = 'rtl'`, native runs `I18nManager.forceRTL(true)`). Native auto-mirrors `flexDirection: 'row'` and physical `textAlign: 'left'/'right'`, but ~41 files had hardcoded `flexDirection: 'row-reverse'` and ~31 files had hardcoded `textAlign: 'right'` — these caused a *double-flip* on iOS/Android (web looked correct because RN-Web with `dir=rtl` doesn't mirror flex the same way). Fix: added `rowDirectionStart` to `src/lib/rtlLayout.ts` (`'row-reverse'` on web, `'row'` on native), narrowed `rtlTextAlignStart` to `'left' \| 'right'` so it satisfies `TextInput.textAlign`, and migrated every hardcoded site to the helpers (plus added `aboutRtlRow` mirror in `features/about-landing/aboutWebRtlStyle.ts`). Closes the textAlign portion of TD-110 (border-radius mirroring on `MessageBubble.tsx` deferred to TD-159). | Closed |
| TD-160 | 🟢 | **Absolute-positioning `start`/`end` corner inset duplicated across ~10 components.** TD-158's absolute-position audit added the `if (Platform.OS !== 'web') return { end: X }; return isLayoutRtl() ? { left: X } : { right: X };` pattern in 10+ files (TopBar, FeedFilterIcon, ChatBadge, AboutNavDrawer, AboutLandingScreen fab, welcomeScreen iconWrap, PhotoPicker removeBtn, search filterBadge, onboarding photo cameraBadge, Buttons iconSlot). Consolidate into `absoluteCornerStart(inset)` + `absoluteCornerEnd(inset)` helpers in `src/lib/rtlLayout.ts` and replace the inline closures. Opportunistic — current copies are tiny and locally correct. | Opportunistic |
| TD-131 | 🟢 | **`react-hooks/rules-of-hooks` — `audit.tsx` early `Redirect` before hooks fixed (2026-05-18).** `app/eslint.config.mjs` uses the plugin default `error` for `react-hooks/rules-of-hooks`; any new violations surface in CI. | Closed |
<!-- ============================================================ -->
<!-- Audit 2026-05-16 — FE rows (TD-100..129)                     -->
<!-- Source: docs/SSOT/audit/2026-05-16/99_consolidated.md          -->
<!-- ============================================================ -->

| TD-104 | 🟠 | **Auth/Profile UX cluster.** Welcome screen has no email entry (FR-AUTH-001 AC2 — only Google + Apple-shell + guest); Forgot Password button is dead (`sign-in.tsx:115-117`, no route); Edit Profile lacks read-only email/phone block (FR-PROFILE-007 AC2, closes followup §5.10); `FR-AUTH-016` 30-day re-registration cooldown unimplemented (`DeletedIdentifier` table absent); Share Profile button no-op (closes TD-106). | P2.x / opportunistic |
| TD-105 | 🟠 | **Auth/Profile small-fixes cluster.** Soft-gate wired only to post-create — follow + chat-message paths bypass `useSoftGate` (FR-AUTH-015); `OnboardingStepHeader` no Back on step 1 (FR-AUTH-018 AC3); avatar resize 512 vs spec'd 1024 (FR-AUTH-011 AC2); SSO avatar never copied to our bucket (FR-PROFILE-015 AC1); followers list non-paginated (FR-PROFILE-009 AC5); `SignInWithEmail` short-circuits `invalid_email` distinct from `authentication_failed` (D-22 spirit); biography regex over-matches `e.g.`/`Node.js`; OnLockPress tappable on other-user profile (FR-PROFILE-012 AC1); AuthGate offline cold-start `isLoading` stuck (FR-AUTH-013 AC3); profile counters not Realtime-subscribed (FR-PROFILE-013 AC5). | Opportunistic |
| TD-106 | 🟢 | **Application-layer error-class hygiene.** ~~`CompleteBasicInfoUseCase` mixes raw `Error('invalid_display_name')` + `OnboardingError`~~ ✅ **closed 2026-06-09** — the six field-validation throws now use the typed `ProfileError` (matching `UpdateProfileUseCase`, same codes; `.message` preserved so the `mapEditProfileSaveError` consumers are unaffected); `OnboardingError` stays for the `illegal_transition` state guard. `SetAvatarUseCase` likewise migrated to typed `ProfileError` (added `invalid_avatar_url` to `ProfileErrorCode`; `invalid_user_id` reused). Remaining: `ResendVerificationEmail` enforces no cooldown (latent — phone OTP not implemented); `editableProfileSupabase.ts` (and the wider rides/* adapter family) throw raw `Error(error.message)` to wrap Supabase **I/O** failures — NOT a domain-validation case; closing piecemeal would be inconsistent, so this needs a shared infrastructure-error type decision before any single-file fix (deferred — documented finding, not fixed). | Opportunistic |
| TD-107 | 🟠 | **Posts/Feed FE cluster.** Missing OnlyMe interstitial (FR-POST-006 AC3); missing owner-mode banners "🔒 Private" / "👥 Followers" on PostDetail (FR-POST-015 AC2/3); `SearchFilters` lacks shared post-shape dimensions `itemConditions`/`locationFilter`/`statusFilter`/`sortOrder` (expansion of TD-136); distance-fallback invisible (UI keeps "By proximity" highlighted while serving newest); image-upload cleanup on cancel/abort of create flow; `applyPostActorIdentityProjection` writes `shareHandle=''` instead of `null` — latent navigability-bypass risk. | Pre-launch hardening |
| TD-110 | 🟠 | **Chat/Notifications FE cluster (narrowed).** **2026-05-28:** three highest-visibility bugs closed: deleted-user inbox over-dedupe (`dedupeRowsByCounterpart` now keys on `chat_id` for null counterparts); unread badge flicker on incoming-while-reading (`reduceBumpInboxForIncomingInsert` accepts `isActiveChat`; `chatStore.bumpInboxForIncomingInsert` reads `useActiveScreenStore` and skips the bump when the viewer is in `/chat/<chatId>`); system-message inbox preview shows raw body (new `formatInboxPreview` adds `(הודעת מערכת)` / `(שיחה חדשה)` prefixes). **Remaining:** bubble tail-notch corners fixed via logical `borderBottomStartRadius`/`borderBottomEndRadius` (TD-159, 2026-06-01) — the `alignSelf: flex-start/end` cross-axis parity between web and native is still unverified (no native runner available); revisit only if a divergence is empirically confirmed; `subscribeToInbox` no status handler → silent realtime drop; optimistic-message dedupe brittle on repeated body (`status='pending' && senderId && body` match — also gate on `clientId`); foreground suppression gap for `support_message` + `system_message` kinds; web tap-handler dormant trap for TD-65 Web Push; client-side race on toggle-then-notification. | Pre-launch hardening |
| TD-111 | 🟢 | **Chat/Notif polish cluster.** Counter visibility threshold (`>=1900` decorative); FR-CHAT-005 AC4 auto-message dedupe by post not enforced; FR-CHAT-016 entry-point reuse pending FR-DONATE-004 wiring; FR-CHAT-016 AC4 tie-break docs gap; chat search niqqud normalize edge case; anchored card gating awkward; admin long-press flicker (`TouchableOpacity` fires onPress after release); FR-NOTIF-016 quiet-hours compliant (no UI surface); FR-NOTIF-012 deletion email absent (TD-118 covers); web register fallthrough silent — add `console.info`; push title/body raw-key fallback risk; OS quick-actions (`categoryIdentifier`/`actions`) absent for `follow_request`. | Opportunistic |
| TD-112 | 🟠 | **Following / moderation FE cluster.** Unfollow confirm copy missing name interpolation (FR-FOLLOW-002 AC1 — "Stop following [name]?"); `FollowState` `UserUnavailable` not surfaced on existing edges with a later-suspended target; followers-list inner `⋮` `TouchableOpacity` nested in row's outer Pressable → modal opens behind the new screen on iOS; ~~`ReportUserModal` uses bare `Alert.alert` (no-op on react-native-web)~~ ✅ **closed 2026-06-09** — migrated to `NotifyModal` (title+body keys added under `moderation.report.user`), matching `ReportPostModal`; ~~`ReportChatModal` missing reset-on-close `useEffect`~~ ✅ **closed 2026-06-09** — all three report modals (post/chat/user) now share a `useReportForm` hook (`components/report/useReportForm.ts`) that owns reason/note/submitting/notify state + the `!visible` reset + the `ReportError`→`NotifyModal` outcome mapping, so the previously copy-pasted submit/reset blocks live once (also drops `new_duplicated_lines_density` back under the SonarCloud 3% gate); auto-removed bubble inline-ban hardcodes `reason='policy_violation'` + `note='auto-removed at threshold'` (audit becomes uniform garbage); remove-follower query-invalidation incomplete (removed party's `['profile-other']` + `['follow-state']` not invalidated). | Pre-launch hardening |
| TD-113 | 🟢 | **EXEC-9 stale i18n strings.** `i18n/locales/he/modules/moderation.ts:110-111` still ships `block_user` / `unblock_user` labels; no code references after EXEC-9. Delete after the block-restoration decision lands. | Post-EXEC-9 cleanup |
| TD-114 | 🟢 | **App-layer + edge-fn contract consistency.** Cross-cuts STAT-5, DON-7, DON-9 (cap-vs-spec divergence; client/edge error-code drift). Tracking row only; close together with TD-97/TD-98. | Opportunistic |
| TD-115 | 🟠 | **About / Settings FE polish.** Instagram embed web-vs-native AC clarity (spec only forbids embed on web — native still uses WebView correctly); language picker intentionally absent under R-MVP-Core-4; About WhatsApp "community" link uses `wa.me/972528616878` direct-chat (same as personal) — replace with stable `chat.whatsapp.com` invite when issued; contact CTA double-entry (About → /settings/report-issue + FR-SETTINGS-008 → same modal) — note in spec. | Pre-launch hardening |
| TD-121 | 🟠 | **`app.json` missing `extra.eas.projectId` → push registration silently skipped.** `registerCurrentDeviceIfPermitted` logs warning and returns; `getExpoPushTokenAsync` never called; **no push notification is ever delivered to a real build** for any local prebuild (`expo run:ios` / `expo run:android`). EAS Build auto-injects, but only via EAS Build itself. Either commit the projectId or surface a user-visible diagnostic in `DevBanner`. | Pre-launch |
| TD-127 | 🟠 | **Routing / cold-start cluster.** (1) Eleven typed-route escapes (`as never` / `as any` / `as unknown as Href`) bypass `expo-router` typed routes — defeats the build-time rename safety. (2) Cold-start auth race: `subscribeToSession` `INITIAL_SESSION` can fire before `restore` resolves; if `restore` then catches a 401 and calls `setSession(null)`, the live session is overwritten → 1-frame flash to `(auth)` before reconciliation. Run `restore` first then attach the subscription, or guard the handler to ignore `null` deltas during restore. | Pre-launch hardening |
| TD-129 | 🟢 | **FE hygiene cluster.** i18next `compatibilityJSON: 'v3'` deprecated (will break at v25 upgrade); dead `+html.tsx` (web.output=single makes it unreachable); duplicated `pickStorage()` across 6 composition roots; `chatStore.ts:150` realtime `onError: () => {}` silent (no `ChatChannelStatus` wiring); CI lint check absent on `dev` (re-asserted FE-26 / §5.11); dual-bar tab navigator pattern needs DECISIONS entry; no English-locale fallback testing or static key-coverage test; raw `throw new Error('not_authenticated')` in screens — extend `check-architecture.mjs` DOMAIN-ERROR-RULE to cover `apps/mobile/`. | Opportunistic |
| TD-101 | 🟡 | **In-app super-admin UI for legal-document editing.** Markdown preview, severity selector, scheduled-publish, diff vs previous version, validation hints. Replaces the Supabase Studio snippet workflow documented in `OPERATOR_RUNBOOK.md`. Reduces editing error rate. Builds on FR-SETTINGS-010 server-side primitives. | Post-MVP |
| TD-102 | 🟡 | **Add date-of-birth field at signup (FR-AUTH-002).** Required to satisfy תיקון 13 heightened expectations around minors (the v1 privacy text already says 13+ but doesn't collect DOB). Combine with parental-consent verification when COPPA path is added. | Pre-launch hardening |
| TD-103 | 🟢 | **"View my consent history" surface in Settings → Privacy.** Reads `user_legal_acceptances` for `auth.uid()` and renders the per-version acceptance log. Lets users self-serve GDPR Art. 7 transparency. | Post-MVP |
| TD-108 | 🟢 | **Wire `useActiveModalReservation` into existing sheets/modals.** Currently the `ModalStackProvider` (introduced by FR-SETTINGS-010) only the `LegalConsentGate` reads from it — `PhotoSourceSheet`, `ClosureSheet`, `PostFilterSheet`, composer modals, etc. do not `push()`/`pop()`. Result: the gate may render over an open sheet on edge cases. Migrate each sheet to call `useActiveModalReservation(visible)`. | Opportunistic |
| TD-132 | 🟢 | **Unmaintained `react-native-markdown-display@7.0.2` pins a vulnerable `linkify-it@2.2.0`.** Dep chain `apps/mobile > react-native-markdown-display@7.0.2 > markdown-it@10.0.0 > linkify-it@2.2.0` triggers advisory GHSA-22p9-wv53-3rq4 (linkify-it `match` quadratic complexity, ReDoS on attacker-controlled markdown; patched only in `>=5.0.1`). No clean override path: `markdown-it@10` expects `linkify-it@^2`, and markdown-it only moved to linkify-it@3+ at v11 (react-native-markdown-display is unmaintained and pins markdown-it@10). Worked around by adding the GHSA to `app/package.json` `pnpm.auditConfig.ignoreGhsas` so the `pnpm audit --audit-level=high` CI gate passes. Proper fix: replace/fork the markdown renderer (or upgrade to a maintained one on markdown-it@14) and drop the ignore. Practical severity low — client-side render-time DoS only. | Pre-launch hardening |
| TD-130 | 🟢 | **FR-RESEARCH-004 share-message body duplicated across two locale modules.** `research.share.shareMessage` (used by public web placements 1+2) and `survey.shareResearch.shareMessage` (used by in-app placement 3) carry the same Hebrew string verbatim. Copy edits require touching both files. Acceptable today (namespace isolation) but a future maintenance trap. Consider extracting to a shared key once the cross-module dependency is acceptable. | Opportunistic |
| TD-133 | 🟢 | **`translate` Edge Function is per-field; no batch endpoint (FR-TRANSLATE-003).** Phase 2 tames feed translation cost via viewport-gating + dwell + session skip-map rather than a true batch call, so a cold visible window still issues one request per `(post, field)`. Add a batch endpoint (multiple fields/posts per invocation) to cut round-trips on fast-scrolling cold feeds and reduce function invocations. `MaterializePostTranslationsUseCase` already iterates misses, so the batching seam is the `ITranslationService` port + Edge handler. | Opportunistic |
| TD-134 | 🟢 | **GloWe forums/discussion still mirror into the community feed via `localStorage`, not `glowe_posts` (FR-GLOWE-008 / GLOWE.B4).** After GLOWE.B3 slice 2 the two community composers (write-post + inline) persist to `glowe_posts` (`post_type='community'`) via `insertOwned`, but `handleForumQuestionSubmit` and `handleDiscussionSubmit` (`app/apps/glowe-web/js/app.js`) still call the legacy `saveCommunityPost(...)` which writes `gloweCommunityPosts` in `localStorage` (+ a dead `apiRequest('/api/posts')`). `getAllCommunityPosts()` therefore still merges `getSavedCommunityPosts()` (localStorage) with the backend-loaded `communityPosts`, and the profile "recent posts" panel (~line 4824) reads the same localStorage. Do not drop the merge or remove `saveCommunityPost`/`getSavedCommunityPosts` until forums/discussion (GLOWE.B4) migrate their feed mirror to backend and the profile panel reads from `glowe_posts`; only then does the community feed become fully backend-sourced. | GLOWE.B4 |
| TD-166 | 🟡 | **Anti-collusion caps (reciprocity + velocity) are a hard precondition for the FR-KARMA-008 public flip.** Karma is self-only at MVP (D-155). Before any public karma display can be enabled, the reciprocity cap (mutual-follow ring farming) and velocity cap (outreach/follow burst farming) must be designed, implemented, and back-tested against the ledger. See D-156 for the formal precondition record. Do not enable public karma without closing this item. | Pre-public-karma |
| TD-167 | 🟢 | **Karma self-realtime uses `postgres_changes` per own-row; upgrade path if subscriber fan-out grows.** `SupabaseUserRealtime` (`packages/infrastructure-supabase`) subscribes to `postgres_changes UPDATE` filtered by `user_id=eq.<userId>`. This pattern fans out O(n-users) Realtime slots at scale. Upgrade path: replace with a Supabase Broadcast channel sourced from a DB function / pg_notify hook so only one channel exists per node and the mobile client listens to a user-scoped broadcast topic. Low priority at MVP scale. | Post-MVP |
| TD-172 | ✅ | **Closed (migration `0199`).** A `BEFORE UPDATE` guard trigger (`posts_guard_client_status_transition`, SECURITY INVOKER so `current_user` reflects the real role) rejects illegal client-direct `posts.status` transitions + `reopen_count` rewrites; the only client-direct transition left is `open → deleted_no_recipient` (close-without-recipient, `closureMethods.ts`). Every other status writer runs as `postgres` and bypasses via `current_user not in ('authenticated','anon')`: `admin_remove_post`, `admin_restore_target`, `rpc_recipient_unmark_self`, the report-effects + `posts_expiry_transition` triggers/cron were already SECURITY DEFINER, and the three closure/reopen RPCs (`close_post_with_recipient`, `reopen_post_marked`, `reopen_post_deleted_no_recipient`) were **converted INVOKER→DEFINER** (each already asserts `auth.uid() = owner`, so authz is unchanged — the audit's "move status writes behind SECURITY DEFINER RPCs" shape). That conversion also fixes a **latent bug**: `reopen_count` is not in 0070's narrow client grant, so the INVOKER reopen RPCs raised `permission denied for table posts` on any DB where the grant was correctly narrow (e.g. a fresh CI stack) — reopen only "worked" on the drifted dev/prod DBs. Bonus: `0199` heals dev-DB catalog drift where `reopen_post_deleted_no_recipient` (created in 0070) was missing from the live `public` schema. Regression test `supabase/tests/0199_posts_status_transition_guard.sql`. | Closed |
| TD-173 | 🟢 | **Security hardening backlog from the 2026-06-14 advisor sweep.** (1) Revoke anon/PUBLIC `EXECUTE` on the ~153 privileged SECURITY DEFINER functions (`admin_*`, `delete_account_data`, `users_get_self_private_fields`, …) — not exploitable (bodies self-gate on `auth.uid()`) but removes pre-auth attack surface + clears 153 advisor warnings. (2) Pin `search_path` on the 24 invoker trigger/helper functions. (3) Enable Auth leaked-password (HIBP) protection. (4) Perf advisors: `auth_rls_initplan` ×96 (wrap `auth.*()` in `(select …)`), `unindexed_foreign_keys` ×32, `unused_index` ×42, `multiple_permissive_policies` on users/admin_role_grants/follow_requests. (5) ✅ **Closed 2026-06-15** — `GrantAdminRoleUseCase` now checks `hasPermission(getMyRoles(), 'admins.grant_role')` and `BanUserUseCase` checks `hasPermission(getMyRoles(), 'reports.permanent_ban')` (both super_admin per `PERMISSION_MATRIX`, matching the DB RPCs), failing an unauthorized caller fast with a typed `forbidden` error; `getMyRoles()` added to `IModerationAdminRepository` + adapter (mirrors the `get_my_admin_roles` RPC). Defense-in-depth, FR-ADMIN-006. | Opportunistic |
| TD-174 | 🟠 | **GloWe dev-DB drift: orphan objects + merged migrations not auto-deployed.** (1) The dev Supabase project (`roeefqpdbftlndzsvhfj`) carries an earlier, ad-hoc GloWe org-approval implementation applied directly via MCP — table `glowe_org_applications` + functions `glowe_admin_approve_org`/`glowe_admin_reject_org`/`glowe_is_admin` — that exists in **no repo migration**. These are orphans (the canonical approach is column-based on `glowe_profiles` via `0205`/`0206`) and would be absent on a fresh CI/prod rebuild; they should be dropped from dev once confirmed unreferenced. (2) Migrations `0205_glowe_onboarding` + `0206_glowe_org_approval` were merged to `dev` (PRs #577/#578) but were **not applied to the dev DB** by any deploy automation — the GloWe Admin UI failed with `PGRST202 (function not found)` until they were applied manually via MCP `apply_migration` this session. Investigate why the dev `db-deploy` path skipped them (numeric-prefix repo files vs. timestamp-versioned MCP ledger entries) so future GloWe migrations deploy on merge. | Pre-launch / DB hygiene |
| TD-135 | 🟢 | **Narrowed 2026-07-05 — profile mission prose + opportunity array chips now translated; only derived composite prose remains.** Both actionable gaps closed: (1) the **profile detail page** (`_renderProfileContent`) now tags its **mission prose** with a DB-profile-only `data-tr-field` resolved at render time via `_adaptDbProfile._tr.missionField` (org→`org_description`‖`about`, member→`about`); static (non-DB) profiles carry no `_tr` so get no unmatchable markup, and `_renderProfileContent` sets `data-tr-card`/`type`/`id` + triggers `GloweTranslate.scan()`. (2) **Opportunity array chips** (`requirements`/`responsibilities`) now translate per element: the driver reads `data-tr-field` verbatim (`requirements.0`), the Edge Function `SOURCE` registry gained `arrayFields` + `resolveField()` (dotted `field.index` → array element, read server-side), and migration `0226` extends `glowe_ct_field_chk` to accept `^(requirements\|responsibilities)\.[0-9]+$` so each chip caches independently (AC4); opp edit-purge trigger extended to fire on array changes. Verified live on `opportunity-glw-c2-vol-001` (translated + cached, he→en). **Residual (by design, not debt):** derived/composite profile prose (`valuesText`, `solutionText`) has no single source column (built from `profile.values`‖type-text‖mission fallbacks) so it stays untranslated; array/tag fields excluded by spec AC4 (opportunity `skills`, wish `areas`, profile `skills`/`languages`, names) are intentionally never translated. | Opportunistic |
| TD-175 | ✅ | **Closed (FR-GLOWE-005 Phase 2).** `GLOWE_TRANSLATIONS.he` extended to ~860 keys covering the chrome of every inner page (Opportunities/Volunteer Network, Organizations, Community, Forums, Wishing Well, About, Terms, Settings, Personal Area, Messages, Admin) plus the shared modals injected by `ensureGlobalUI()` (Share a Wish, Offer Support, Edit Profile, Add Project, Report) and onboarding, and the Terms legal prose. Strings were harvested by walking the live DOM of all 17 pages (incl. modal templates) and subtracting the existing dict + `data.js`. Verified in-browser: inner pages render `dir="rtl"`/`lang="he"` with only `data.js` seed demo content remaining English. **Residual (by design, not debt):** `data.js` seed/demo content (sample org & opportunity names/descriptions, demo posts, composed aria-labels) stays English — it is transient placeholder data replaced by real backend content, so translating it would only bloat `app.js`. | Closed |

### Process · docs · tooling

| ID | Sev | Item | Closing slice |
| -- | -- | ---- | ------- |
| TD-9 | 🟢 | `android/` is gitignored (CNG workflow). Must run `expo run:android` with `JAVA_HOME=.../temurin-17.jdk`. Pinned in `package.json android` script. If CI added, set `JAVA_HOME` env var there too | When CI lands |
| TD-11 | 🟠 | `post-images` storage bucket is public-read. For `OnlyMe`/`FollowersOnly` posts we rely on URL non-discoverability (post row hidden by RLS, image paths not enumerable). Replace with per-object signed URLs (or private bucket + sign-on-fetch) at scale. CDN still pending (Wave 4). **2026-05-28 update:** the `?width=…&quality=…` transform path noted in the prior revision was reverted by PR #397 (Supabase Storage `/render/image/` endpoint hangs / 404s on this project). PERF-4 replaces it with sibling `-thumb.jpg` objects written at upload time + a one-time backfill Edge Function for legacy objects (`backfill-image-thumbs`, see `OPERATOR_RUNBOOK.md`). Both buckets now serve with `cache-control: public, max-age=31536000, immutable`. **2026-06-14 (security audit): elevated 🟢→🟠** — live-confirmed the `storage.objects` SELECT policy is `using (bucket_id = 'post-images')` for `{anon,authenticated}`, so anon can not only read but **LIST/enumerate every object** (paths embed the owner uid), defeating the URL-non-discoverability assumption for `OnlyMe`/`FollowersOnly` images. Signed-URL remediation is now higher priority. See `docs/superpowers/audits/2026-06-14-security-and-db-integrity-audit.md` (Finding 2). | Pre-launch hardening |
| TD-30 | 🟠 | No JSDoc / TSDoc on most public exports across `domain`, `application`, `infrastructure`, mobile components | Opportunistic |
| TD-31 | 🟢 | **Test coverage — repos / infra adapters / invariants / stores all covered.** Remaining gap is React components + hooks (gated by TD-150, no RNTL infrastructure). Status as of 2026-05-16: `@kc/domain` 37 tests, `@kc/application` 340+ tests, `@kc/infrastructure-supabase` 660+ tests, `@kc/mobile` lib/stores/notifications 213+ tests — total ~1250 passing. Close once TD-150 ships and a first wave of component tests lands. | Pre-launch hardening / blocked on TD-150 |
| TD-36 | 🟢 | `SRS/appendices/A_traceability_matrix.md` referenced as FR ↔ R-MVP ↔ Screen ↔ Test mapping — needs population audit | Opportunistic |

---

## How to add a new TD

1. Pick the next free ID in your lane (BE: `TD-50..99`, FE: `TD-100..149`).
2. Add a row under the right Active section with severity, item, and closing slice.
3. If the TD is blocking the current feature, also link it from the relevant `spec/{domain}.md` AC when the feature ships.
4. When closing: simply delete the row. Git history is the cross-reference.
